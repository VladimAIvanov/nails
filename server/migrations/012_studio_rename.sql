-- Студия переименована: «Варвара» → бьюти-студия «Ноготочки».
--
-- Название живёт в studio_settings.title, строку кладёт 002_reference.sql.
-- Применённую миграцию не правим — она уже записана в schema_migrations
-- и второй раз не выполнится, поэтому переименование отдельным шагом.
-- Слово «бьюти-студия» в поле не пишем: его добавляют сами шаблоны
-- (подвал сайта, календарь), в title хранится только имя.
--
-- Условие по старому названию: если студия уже сменила его в панели,
-- её выбор не перетираем.
UPDATE studio_settings SET title = 'Ноготочки' WHERE id = 1 AND title = 'Варвара';
