/* Страница отказа. Показывается, когда сервер ответил 403 на административный
   адрес: человек вошёл, но роли администратора у него нет.

   Своих запросов у неё нет — только шапка, чтобы страница не выглядела
   выпавшей из сервиса. Текст объяснения в разметке: он не зависит от данных
   и не должен ждать ответа сервера, чтобы появиться. */
import { renderHeader } from './header.js';

renderHeader();
