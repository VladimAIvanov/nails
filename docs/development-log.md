# Журнал разработки

Документ для тех, кто продолжит работу над проектом — людей и ИИ-ассистентов в новых сессиях. Здесь фиксируются решения, у которых были альтернативы, проблемы, которые заняли время, и особенности окружения. Пересказа кода тут нет: код читается сам, а вот причины, по которым он такой, из него не восстановить.

Дополняется по мере развития проекта. Последняя запись — снизу каждого раздела.

## Где что лежит

| Документ | О чём |
|---|---|
| [../README.md](../README.md) | С чего начать: что установить и как запустить проект локально |
| [db-schema.md](db-schema.md) | Схема базы: таблицы, поля, связи, ограничения, индексы, разбор спорных решений |
| [deployment.md](deployment.md) | Требования к окружению и риски переноса на сервер |
| [Паспорт продукта](Паспорт%20продукта.docx) | Цели, роли, функции, сценарии |
| [Карта связей](Карта%20связей.dc.html) | Семь страниц прототипа и переходы между ними |

Код: `server/src/db.js` — подключение, `server/src/migrate.js` — прогон миграций, `server/src/seed.js` — тестовые данные, `server/migrations/*.sql` — сама схема. Черновые страницы для проверки API — в `web/`, к ним своя [инструкция](../web/README.md).

## Состояние на 23 августа 2026

Готово: схема базы, миграции, тестовые данные, проверки окружения и API — 101 адрес, все покрыты проверками. Пройдены аудит прав, проверка безопасности по пяти зонам и подготовка к публичному адресу; критичные находки отчёта закрыты (раздел 16). Поверх API собран **черновой** фронтенд в `web/` — формы и таблицы для ручной проверки, без дизайна (раздел 17). Настоящий интерфейс планируется на Vite + React с CSS дизайн-системы из `site/`; сама папка `site/` — артефакт прототипирования и не трогается.

---

## 1. Как устроена база и как она связана с логикой проекта

Схема выведена из карты связей прототипа: разобран каждый экран и записано, какие данные он показывает. Ничего «на будущее» в схеме нет.

**Ядро — три сущности и связь между ними.** `users` содержит всех людей с полем роли: клиенток, мастеров и владелицу. `services` — прайс. `appointments` связывает клиентку, мастера и услугу во времени. Всё остальное обслуживает это ядро.

**Групп пользователей ровно три**, и набор закреплён проверкой прямо в колонке: `CHECK (role IN ('client', 'master', 'admin'))`. Четвёртую роль база не примет.

| Группа | Значение | Что доступно | В тестовых данных |
|---|---|---|---|
| Клиент | `client` | Запись, свои визиты, профиль. Может не иметь пароля — гостевая запись | 3 |
| Мастер | `master` | Своё расписание и свои записи. Прайс и настройки студии недоступны | 3 |
| Администратор | `admin` | Вся студия: расписание всех мастеров, все записи, прайс, настройки | 1 |

Роль — это не только метка доступа, от неё зависит структура данных: строка в `master_profiles` может существовать только для `master`, а `studio_settings.owner_user_id` указывает на пользователя с ролью `admin` — именно ему уходит уведомление о новых записях.

**Роль мастера защищена базой, а не кодом.** Витринные данные мастера вынесены в `master_profiles`, и ссылка оттуда на `users` составная — `(user_id, role) → users(id, role)` при `CHECK (role = 'master')`. Поэтому клиентку физически нельзя записать в профили мастеров. А так как `appointments.master_id` ссылается на `master_profiles`, а не на `users`, гарантия распространяется дальше: записать клиентку к клиентке невозможно.

**Свободное время не хранится.** Таблицы слотов нет намеренно: заранее нарезанные окна пришлось бы пересоздавать при каждом изменении графика, и любой сбой синхронизации выдавал бы занятое время как свободное. Окна вычисляются в момент запроса из графика (`working_hours` + `schedule_exceptions`), существующих записей и блокировок (`time_off`). Алгоритм — раздел 5 схемы, семь шагов.

**Главная гарантия — запрет двух записей к одному мастеру внахлёст.** Реализована двумя триггерами, `BEFORE INSERT` и `BEFORE UPDATE`. Условие пересечения — строгие неравенства с обеих сторон, поэтому запись встык в момент окончания предыдущей проходит, а наложение отклоняется.

**Снимки против ссылок.** Цена и длительность копируются в запись при создании и дальше живут своей жизнью. Это не дублирование, а фиксация факта сделки: изменение прайса не должно переписывать историю визитов и отчёты за прошлые месяцы.

**Что вычисляется, а не хранится.** Рейтинг мастера — из `reviews` через представление `master_ratings`. Подпись на карточке мастера («Маникюр, наращивание, дизайн») — из списка его услуг через категории. Свободные окна — по алгоритму. Всё это производные величины, и хранить их означало бы заводить второй источник истины.

---

## 2. Решения, у которых были альтернативы

Полный разбор — в разделах 8 и 11 схемы (20 пунктов и 7 развилок). Здесь — те, что важны для дальнейшей работы.

### SQLite вместо PostgreSQL

Изначально схема писалась под PostgreSQL, и главным доводом был `EXCLUDE USING gist` — ограничение, запрещающее пересечение записей одной декларативной строкой. Позже требование поменялось на SQLite.

Что пришлось заменить: `EXCLUDE` → триггеры, типы-перечисления → `TEXT` с `CHECK`, `timestamptz` → строка ISO-8601 UTC, `UNIQUE NULLS NOT DISTINCT` → уникальный индекс по `COALESCE(master_id, 0)`, `citext` → `TEXT COLLATE NOCASE`, материализованное представление → обычное, `pg_trgm` → `LIKE` и обычный индекс.

**Что потеряно честно:** декларативность защиты (триггер можно обойти массовой загрузкой или отключить) и возможность горизонтального масштабирования — в SQLite один писатель.

**Что не потеряно:** сама гарантия против двойной записи. Писатель один, транзакции открываются через `BEGIN IMMEDIATE`, блокировка файловая — значит работает и между процессами. Гонки «две клиентки заняли одно окно» не возникает.

Возвращаться к PostgreSQL стоит, если появится второй филиал или понадобится несколько одновременно пишущих процессов. Логика схемы от движка не зависит — меняются DDL и три механизма.

### Одна таблица `users` вместо трёх по ролям

Экран входа один на три роли, поэтому логин, пароль и телефон живут в одном месте. Плата — пустые `email` и `password_hash` у клиенток; часть пустот закрыта проверками `CHECK`. Двухслойная модель (`accounts` + профили) станет оправданной, если ролей станет больше трёх или у людей появятся две роли сразу; переключиться на ходу нельзя, это разовая миграция.

### Гостевая запись без регистрации

Карта связей прямо разрешает записаться без входа, поэтому `password_hash` у клиенток может быть пустым — так и есть у двух из трёх в тестовых данных. Обратная сторона: знающий чужой телефон попадёт в чужой профиль. Для этого заведён переключатель `studio_settings.guest_booking_mode` со значениями `open`, `verify_by_code`, `require_account` — модель поведения меняется без изменения схемы.

### Что убрано как дублирование

`reviews.client_id` и `master_id` (берутся из записи), поля отмены в `appointments` (живут в журнале статусов), `master_profiles.specialization` (собирается из прайса), флаг подписки на окна в настройках уведомлений (состояние — наличие строки в `slot_subscriptions`). Каждое из них однажды разошлось бы с источником.

---

## 3. Проблемы, которые заняли время

### Вычисляемое поле не создавалось в PostgreSQL

`CREATE TABLE` падал с `42P17`: «генерирующее выражение не является постоянным». Причина неочевидная: оператор `timestamptz + interval` помечен STABLE, потому что интервал может содержать месяцы и дни, а их длина зависит от часового пояса. Решение — обёртка `appointment_end()` с пометкой `IMMUTABLE`: для интервала, заданного только в минутах, это фиксированное смещение в секундах, и пометка не обман планировщика. После перехода на SQLite вопрос снялся, но приём стоит помнить.

### `pragma_table_info` не показывает генерируемые столбцы

Проверка полноты схемы уверенно сообщила, что `appointments.ends_at` есть в документе, но нет в базе. Оказалось, генерируемые столбцы для `table_info` скрыты — нужен **`pragma_table_xinfo`**, там поле видно с признаком `hidden=3`. Ошибка была в проверке, а не в схеме. Любые сверки схемы делать через `table_xinfo`.

### Разборщик документа спотыкался на пустых ячейках

Первый прогон сверки выдал 139 расхождений, все ложные. Шаблон требовал пробелов вокруг разделителей markdown-таблицы, а строки с пустой колонкой «Ключ» записаны как `| |`. Из 194 полей разбирались 55. Вывод общий: прежде чем верить отчёту проверки, стоит убедиться, что она вообще видит данные.

### Одна соль на все учётные записи

Пароли хешировались scrypt, но `hashPassword()` вызывался один раз, и результат переиспользовался для всех. В дампе было видно прямо: у админа и двух мастеров совпадали и соль, и хеш. Формально хеширование есть, фактически смысл соли сведён к нулю — по совпадающим хешам видно, что пароль общий, и подбор идёт разом по всем. Исправлено: функция вызывается отдельно для каждой записи.

### Путь к базе разворачивался не оттуда

`SQLITE_PATH=server/data/nogotochki.db` разрешался относительно текущей папки, а скрипты запускаются из `server/` — получалось `server/server/data/nogotochki.db`, то есть вторая пустая база рядом с настоящей. Плюс `.env` вообще не читался: при переходе на SQLite из npm-скриптов пропал `--env-file`. Теперь путь разворачивается относительно корня проекта, а скрипты грузят `../.env` через `--env-file-if-exists`.

### Пароль PostgreSQL в переписке

При диагностике подключения я вывел содержимое `.env` целиком, включая пароль роли. Роль сменена, строки из `.env` удалены, в историю git файл не попадал ни разу. На будущее: при разборе `.env` выводить только имена переменных.

---

## 4. Драйвер SQLite

**Используется `node:sqlite` — модуль, встроенный в Node.js.** Внешних библиотек в проекте нет: секции `dependencies` в `server/package.json` нет вовсе, `node_modules` пуст, `package-lock.json` отсутствует.

Почему выбран он:

| | `node:sqlite` | `better-sqlite3` | `sqlite3` |
|---|---|---|---|
| Установка | входит в Node | нативная сборка или готовый бинарник | нативная сборка |
| Зависимости | нет | есть | есть |
| Сборка на сервере | не нужна | нужен компилятор или совпадение платформы | то же |
| Модель | синхронная | синхронная | асинхронная, колбэки |
| Стабильность API | **release candidate** | стабильный | стабильный |

Решающим стало отсутствие нативной сборки: на shared-хостинге компилятора может не быть, а несовпадение версии Node или архитектуры с готовым бинарником даёт ошибку `NODE_MODULE_VERSION` при старте — классическая проблема развёртывания. Синхронная модель для SQLite уместна: обращения к локальному файлу быстрые, а код без колбэков читается проще.

**Главный минус, о котором нужно помнить.** У `node:sqlite` уровень стабильности **1.2 — release candidate**. Флага он больше не требует, но API формально может измениться в Node 26. Отсюда правило: мажорная версия Node закрепляется и обновляется осознанно.

**Запасной вариант.** Если на целевом хостинге не окажется Node ≥ 22.13, драйвер меняется на `better-sqlite3` — API близок (`prepare`, `run`, `all`, `get`), правки затронут только `server/src/db.js`. Схема, миграции и сиды не меняются вовсе.

---

## 5. Требования к окружению и деплой

Подробно — в [deployment.md](deployment.md). Здесь главное.

**Node.js: минимум 22.13.0, рекомендуется 24 LTS.** Минимум определяется тремя вещами: модуль `node:sqlite` появился в 22.5.0, флаг `--experimental-sqlite` перестал требоваться в 22.13.0, флаг `--env-file-if-exists` появился в 22.9.0.

Версия закреплена в трёх местах: `.nvmrc` (24.19.0), `engines.node` в `server/package.json` (`>=22.13.0`) и константы в `server/src/env-check.js`. Источник истины — последний: только он проверяется во время работы.

### Про BeGet — проверить до деплоя

**Это главный риск развёртывания.** На shared-хостинге доступны только те версии Node, которые предоставляет провайдер, и выбрать произвольную нельзя. Порядок действий:

1. В панели управления посмотреть список доступных версий Node.js.
2. Если есть **22.13 или новее** — всё работает как есть.
3. Если максимум **22.12 или ниже** — проект не запустится, `node:sqlite` там либо отсутствует, либо требует флага. Варианты: перейти на VPS со своей версией Node, либо заменить драйвер на `better-sqlite3` (правки только в `db.js`).

Проверка занимает одну команду на сервере:

```bash
node -e "require('node:sqlite'); console.log('node:sqlite доступен, Node', process.version)"
```

Ещё две особенности shared-хостинга:

- **Node может быть собран без SQLite** (ключ `--without-sqlite`). Версия подойдёт, а модуля не будет. Проверка `checkSqliteModule` в коде ловит именно этот случай.
- **Каталог базы должен быть на локальном диске и доступен на запись.** Режим WAL создаёт рядом файлы `-wal` и `-shm`, поэтому права нужны каталогу, а не только файлу. На сетевых дисках (NFS, SMB) базу держать нельзя — файл повредится.

Проверить готовность сервера одной командой:

```bash
cd server && npm run doctor
```

---

## 6. Команды

```bash
cd server && npm run migrate   # применить непрогнанные миграции
cd server && npm run seed      # загрузить тестовые данные (идемпотентно)
cd server && npm run reset     # удалить базу, создать заново, применить всё и загрузить данные
cd server && npm run doctor    # отчёт о готовности окружения
```

`npm install` не нужен — зависимостей нет.

---

## 7. Проверенные факты

Состояние на 22 августа 2026, все проверки прогонялись на живой базе.

| Что | Результат |
|---|---|
| Таблицы против `db-schema.md` | 21 из 21, состав совпадает |
| Поля и типы | 194 поля, имена и типы совпадают |
| Внешние ключи | 30, у каждого задан `ON DELETE`: CASCADE 18, SET NULL 7, RESTRICT 5 |
| `PRAGMA foreign_keys` | включается в `db.js`, в миграциях `PRAGMA` нет |
| Индексы | 21 явный, 14 автоматических, 9 частичных |
| Сиды дважды подряд | количество строк не меняется, дублей нет |
| Пароли | scrypt, 120 символов, соль у каждого своя; полей для открытого пароля в схеме 0 |
| `.env` и файл базы | в `.gitignore`, не отслеживаются, в историю не попадали |
| Внешние библиотеки SQLite | отсутствуют |

Проверка версии Node испытана откатом: на 22.12.0 запуск падает с внятным сообщением о требуемой версии, а не с ошибкой в глубине кода.

---

## 8. API (добавлено 22 августа 2026)

Собран HTTP-сервер на `node:http` — зависимостей по-прежнему ноль. Файлы: `index.js` (сервер и маршрутизация), `http.js` (разбор запроса, коды ответов), `validate.js` (проверка входных данных до обращения к базе), `auth.js` (пароли и сеансы), `slots.js` (расчёт свободного времени), `time.js` (UTC и пояс студии), `routes/*.js`.

**Решения, у которых были альтернативы.**

*Свой роутер вместо Express или Fastify.* Всё, что нужно от фреймворка, — разбор пути с параметрами и JSON-тела, это сотня строк. Взамен сохраняется главное свойство проекта: `npm install` не нужен вовсе, а значит нечему сломаться при развёртывании. Если появится нужда в загрузке файлов или websockets, вернуться к Fastify можно без переделки маршрутов.

*Токен в заголовке `Authorization: Bearer`, а не в cookie.* Клиентов трое — сайт, мини-апп Telegram и панель, — и для мини-аппа cookie неудобны. В базе лежит только SHA-256 от токена: утечка таблицы не даёт войти.

*Визит из нескольких услуг — цепочка записей.* Схема хранит одну услугу на запись (раздел 4.9), а расчёт окон должен учитывать суммарную длительность. Компромисс: `POST /api/appointments` принимает список услуг и создаёт записи подряд в одной транзакции. Альтернатива — добавить в схему связь «многие ко многим» — отвергнута: она сломала бы снимок цены и длительности, на котором держится история.

*Уборка после визита считается один раз.* При нескольких услугах длительности складываются, а буфер берётся наибольший из выбранных: кресло убирают после визита целиком, а не после каждой услуги.

**Удержание слота.** Новая таблица `slot_holds` (раздел 4.22 схемы). Это не заранее нарезанные слоты: строка появляется, когда конкретный человек выбрал время, живёт `hold_ttl_min` минут и исчезает. Истёкшие удаляются лениво, перед каждым расчётом окон. Своё удержание при создании записи снимается и уступает место записи, чужое — даёт 409.

Решение противоречит более раннему выводу в схеме, где мягкая бронь была отвергнута со ссылкой на паспорт продукта. Требование пришло позже; отказ на подтверждении остался последней линией защиты, если удержание истекло.

**Проблема, которая заняла время: `npm run reset` под работающим сервером.** На Windows файл базы, открытый другим процессом, не удаляется — `EPERM`. Сначала я заглушил вывод команды и не заметил, что она упала: база осталась старой, а тест ловил «лишние» записи и давал непонятные расхождения. Вывод двойной. Первый: не глушить вывод команд, меняющих состояние. Второй: `reset.js` теперь ловит `EPERM`/`EBUSY` и прямо говорит остановить сервер, потому что пересоздавать базу под работающим сервером нельзя — он продолжит писать в старый файл.

**Проверено сквозным сценарием:** 51 проверка, все коды ответов из требований (400, 401, 403, 409, 404), хеш пароля наружу не отдаётся, клиентка не видит чужих данных и служебных полей, удержание убирает окно из выдачи, отмена возвращает его обратно, удаление услуги и мастера с записями упирается в внешний ключ и отвечает 409 с подсказкой.

## 9. Защита от двойной записи (добавлено 22 августа 2026)

Риск из паспорта продукта закрыт на трёх уровнях. Ключевая мысль: проверка «свободно ли время» на стороне приложения не работает в принципе, потому что между проверкой и вставкой проходит время, и второй запрос успевает пройти ту же проверку.

**Уровень 1 — триггеры в базе.** `appointments_no_overlap_insert` и `appointments_no_overlap_update`. Второй нужен потому, что запись можно создать на свободное время, а потом перенести на занятое; в списке `UPDATE OF` перечислены `starts_at`, `duration_min`, `master_id`, `status`, `allow_overlap`. Условие пересечения — строгие неравенства с обеих сторон, поэтому визиты встык разрешены. Отменённые записи в проверку не попадают.

Триггер срабатывает **при любом способе записи**: через API, из консоли `sqlite3`, из скрипта миграции. Проверено прямой вставкой мимо API — отклонена.

**Уровень 2 — транзакция `BEGIN IMMEDIATE`.** Обычный `BEGIN` в SQLite откладывает взятие блокировки до первой записи: транзакция стартует в режиме чтения и превращается в пишущую только при `INSERT`. В этот момент возможен отказ `SQLITE_BUSY` и, что хуже, между чтением и вставкой успевает вклиниться другой писатель. `BEGIN IMMEDIATE` берёт блокировку записи сразу, поэтому вся последовательность «проверили удержания — сняли своё — вставили запись» неразрывна. Второй писатель ждёт до `busy_timeout` (5 секунд) и получает свою очередь уже после коммита первого — а там его встретит триггер.

**Уровень 3 — обработка в API.** Ошибка триггера ловится по подстроке `appointments_no_overlap` и превращается в 409 с понятным текстом и списком ближайших свободных окон этого мастера (`nearestFreeSlots` в `slots.js`). Текст ошибки базы наружу не уходит: пользователю нужен не `RAISE(ABORT)`, а что делать дальше.

### Разрешённое наложение

Отдельное действие из прототипа: клиентка пришла без записи, мастер согласилась принять, администратор оформляет визит поверх занятого времени. Реализовано признаком `appointments.allow_overlap`.

Тонкость, ради которой это работает правильно: признак гасит проверку **только для самой создаваемой записи**. В подзапросе триггера существующие строки по нему не фильтруются, поэтому наложенная запись дальше занимает время как обычная — третью на тот же интервал база уже не пропустит.

**Где проверка роли.** Признак читается ровно в одном месте — обработчике `POST /api/admin/appointments` в `routes/admin.js`, первая строка которого `requireRole(req, 'admin')`. В клиентском `POST /api/appointments` поле `allow_overlap` не читается вовсе: его нет в списке разбираемых полей и нет в `INSERT`, поэтому столбец получает значение по умолчанию `0`. Клиентка, приславшая `allow_overlap: true`, получит обычный 409 — проверено.

Мастер тоже не может: административный адрес отвечает ему 403.

## 10. Один путь записи для трёх ролей (добавлено 22 августа 2026)

Запись создают три роли с разными интерфейсами, и это естественным образом рождало отдельные обработчики. К моменту проверки их было два — клиентский и административный, — каждый со своим `INSERT`, своей транзакцией и своей обработкой отказа триггера. Мастер создать запись не мог вовсе. Расхождение уже началось: в административном пути не проверялись удержания слотов.

**Сведено в `server/src/services/appointments.js`.** Там четыре функции: `createAppointment`, `rescheduleAppointment`, `cancelAppointment`, `confirmAppointment`. Обработчики маршрутов стали тонкими: разобрать вход, решить, какие поля вообще принимать от этой роли, вызвать общую функцию.

Права решаются внутри `resolveCreatePermissions`, которая по роли возвращает разрешённый набор полей:

| Роль | client_id | master_id | allow_overlap | Запас по времени |
|---|---|---|---|---|
| Клиент | всегда свой | из запроса | всегда 0 | проверяется |
| Мастер | из запроса | всегда свой | всегда 0 | не проверяется |
| Администратор | из запроса | из запроса | из запроса | не проверяется |

Ключевая мысль: подмена невозможна не потому, что где-то стоит проверка, а потому что значение из запроса до `INSERT` просто не доходит — оно перезаписывается в одном месте. Клиент, приславший чужой `client_id`, запишет себя; мастер, приславший чужой `master_id`, запишет к себе. Проверено.

**Единственное исключение — `seed.js`.** Сиды вставляют записи напрямую, и это осознанно: они создают историю, состоявшиеся и отменённые визиты в прошлом, которые обычное создание записи справедливо не пропустит. Защита при этом не обходится — триггеры в базе срабатывают и там.

## 11. Уведомления, регулярные записи, календарь (добавлено 23 августа 2026)

Четыре возможности, три новые таблицы, миграция 005.

**Каналы уведомлений.** `notifications.channel` расширен до пяти: `telegram`, `sms`, `email`, `push`, `whatsapp`. Ограничение `CHECK` в SQLite менять нельзя, поэтому таблица пересобрана переносом данных. Пуши требуют устройств — таблица `push_devices`. Это единственное место в схеме, где секрет хранится не хешем: токен устройства нужно предъявить службе доставки целиком.

Канал не навязывается: функция `channelsFor` берёт пересечение того, что человек включил, и того, чем до него можно достучаться. Если отключено всё, остаётся `sms` — напоминание о визите не рассылка, от него не отписываются.

**Регулярные записи.** Серия — правило, а не набор записей. Визиты создаются вперёд на `generate_ahead` повторов **той же функцией `createAppointment`**, поэтому проверка пересечений, снимок цены и журнал статусов работают как у обычной записи. Занятый повтор пропускается с причиной и не роняет серию целиком.

Решение, у которого была альтернатива: остановка серии по умолчанию **сохраняет** уже созданные записи. Клиентка на них рассчитывает, и молча отменять полгода визитов нельзя. Отменить можно явно — `?cancel_upcoming=true`.

**Календарь.** Два способа: файл на одну запись и личная лента, которую календарь перечитывает сам. В событие вложено напоминание `VALARM` за два часа — оно сработает независимо от того, дошло ли наше сообщение. Лента открывается по токену без входа, потому что приложение календаря не умеет логиниться; токен отзывается отдельно от пароля.

**Отмена по ссылке.** Токен на каждую запись, а не идентификатор: по номеру записи перебором отменяли бы чужие визиты. Переход по ссылке ничего не отменяет — `GET` показывает, что именно отменяется, отменяет только `POST`. Иначе предпросмотр письма или антивирус, открывающий ссылки, отменил бы визит за клиентку.

**Проблема, которая заняла время.** Маршрут `/api/calendar/:token.ics` не работал: мой роутер разбирает сегмент целиком и создаёт параметр с именем `token.ics`, а код читал `params.token`. Лента отдавала ноль событий. Расширение в шаблоне маршрута теперь не пишется, а суффикс снимается в обработчике. Урок общий: в самодельном роутере параметр занимает весь сегмент, дописать к нему литерал нельзя.

## 12. Работа студии (добавлено 23 августа 2026)

Восемь возможностей, десять таблиц, миграция 006. Все опираются на уже существующие записи и ничего в них не меняют.

**Ключевой узел — завершение визита.** Появился `completeAppointment`: до него визит был намерением, после — фактом. В одной транзакции списывается абонемент, начисляются баллы, расходуются материалы по нормам услуги. Отдельно ставится просьба оценить визит — через сутки, а не в дверях.

**Решения, у которых были альтернативы.**

*Баланс баллов и остаток материалов не хранятся полями, а считаются суммой событий.* Хранимый счётчик разошёлся бы с реальностью при отмене или правке, и пришлось бы гадать, чему верить. У материалов поле `stock_qty` осталось, но как кеш: пересчитывается из движений при каждом списании.

*Использование абонемента — отдельные строки, а не счётчик.* По той же причине: строку удаляют вместе с отменённым визитом, счётчик пришлось бы уменьшать вручную.

*Вознаграждение мастера считается из записей, отдельной таблицы начислений нет.* Она была бы копией того, что и так выводится запросом. Процент лежит в `master_profiles.commission_percent`.

*Депозит — только учёт.* Поля «сколько нужно» и «внесено ли». Приём денег делает касса или платёжный сервис; сервис записи их не трогает. После состоявшегося визита депозит считается использованным, после неявки — удержанным: ради этого он и брался.

*Лист ожидания отличается от подписки на окна наличием срока.* «Хочу к Анне на этой неделе» против «сообщите, когда у Анны что-нибудь появится». При отмене освободившееся время сверяется с активными заявками по услуге, мастеру, дате и времени суток.

**Права, которые стоит помнить.** Карточку клиентки видит только студия, причём мастер — лишь тех, кто к нему приходил. Мастер видит своё вознаграждение, но не чужое. Общая аналитика — только администратору. В публичных отзывах нет ни имени клиентки, ни даты визита: по ним восстанавливается, кто и когда приходил.

**Проблема, которая заняла время.** Аналитика падала с `SQL logic error` и сообщением «near "returning"». Псевдоним `AS returning` в подзапросе — зарезервированное слово SQLite: с версии 3.35 это клауза `RETURNING`. Переименовал в `repeat_clients`. Урок: имена, которые кажутся обычными английскими словами, стоит проверять по списку ключевых слов движка.

## 13. Аудит API и доработка (добавлено 23 августа 2026)

Проверка списка адресов против схемы показала главный перекос: **сущности были, управлять ими было нельзя**. График работы, исключения по датам, блокировки времени, настройки студии, категории, тексты лендинга, портфолио, избранные мастера, подписки на окна, согласия — всё это существовало в базе и участвовало в расчётах, но правилось только руками в файле базы. Адресов стало 69 → 101.

**Что добавлено.** Настройки студии (экран «Настройки» из прототипа наконец имеет за собой API), недельный график с полной заменой вместо правки по одному интервалу, исключения по датам, блокировки времени с предупреждением о задетых записях, категории, тексты лендинга, портфолио, поиск клиентов для панели, отметка о депозите, профиль клиента с избранным, подписками и согласиями, смена пароля, список и отзыв сеансов, карточка одного мастера.

**Найденные ошибки.**

*Смена пароля не закрывала сеансы, открытые в ту же секунду.* Проверка `issued_at >= password_changed_at` при точности времени до секунды даёт истину, если сеанс создан в ту же секунду. То есть смена пароля после утечки могла оставить украденный сеанс живым. Теперь сеансы отзываются явным `UPDATE`, а отметка времени осталась второй линией защиты. Урок общий: не полагаться на сравнение времени там, где нужна достоверность, если разрешающая способность времени сопоставима с длительностью операции.

*Отсутствие обязательного параметра давало 404 вместо 400.* Запрос слотов без `service_id` отвечал «не найдено», хотя адрес существует и дело в запросе.

*Проверка наличия параметра шла после его разбора.* В `/api/passes` и `/api/loyalty` студия без `client_id` получала «некорректный client_id» вместо понятного «укажите client_id».

*Депозит после состоявшегося визита помечался возвращённым.* Он засчитывается в оплату, а не возвращается. Возврат и удержание — отдельные события, их отмечает студия.

*Лист ожидания предлагал освободившееся время тому, кто его сам освободил.*

**Что добавлено сверх исправлений.** CORS с белым списком источников из переменной окружения — фронтенд планируется отдельным приложением, без этих заголовков браузер не покажет ответ. И ограничение попыток входа: восемь за пятнадцать минут на логин, дальше 429 с `Retry-After`. Счётчик в памяти процесса, не в базе: задача не заблокировать навсегда, а сделать перебор слишком медленным.

**Полное тестирование.** Шесть наборов, **251 проверка**, все проходят на чистой базе. Основной набор проверяет покрытие: список адресов берётся из `GET /api`, каждый вызов помечается, в конце сверяется, что не вызванных не осталось — 101 из 101.

**`.gitattributes`.** Добавлен: `text=auto eol=lf` для всего, `binary` для документов и картинок, `linguist-vendored` для прототипа. Это заодно убирает предупреждения git про CRLF, которые сопровождали каждый коммит, и защищает от файлов с CRLF на сервере.

## 14. Безопасность (добавлено 23 августа 2026)

Проверка по пяти зонам: пароли, токены, права, секреты, пользовательские данные. Подробный отчёт с остающимися рисками — в `security-review.local.md`, он намеренно **не** в репозитории: перечень слабых мест не стоит публиковать вместе с кодом.

**Проверка началась с фактов, а не с кода.** Обычный клиент запросил список всех записей салона и чужую запись по идентификатору — оба раза 403, данных не отдано. Попытка передать `role: admin` при регистрации проигнорирована. Права с самого начала проверялись на трёх уровнях: авторизация, роль, принадлежность объекта.

**Главное изменение — роли стали списком.** Владелица студии, которая сама принимает клиенток, должна быть одновременно администратором и мастером; с единственным полем ей пришлось бы завести вторую учётную запись, а это два пароля и две точки утечки. Заведена таблица `user_roles`, проверка — пересечение со списком. `users.role` осталась основной ролью: на неё опирается составной внешний ключ, не пускающий клиентку в профили мастеров.

Старшинство ролей задано явно: администратор сильнее мастера, мастер сильнее клиента. Введены `isAdmin`, `isMaster`, `isMasterOnly`, `actsAsClient` — сравнивать `user.role === '…'` больше нельзя нигде.

**Ошибка, которую я допустил по дороге, и она поучительна.** Сорок мест со сравнением роли я заменил сплошной текстовой заменой. Она превратила локальные переменные в самоссылающиеся объявления `const isAdmin = isAdmin(actor)` и заслонила импорт. Сервер начал отвечать 500 на отмену, перенос и подтверждение записи. Поймала регрессия: 38 проваленных проверок из 263. Вывод: сплошная замена в коде без немедленного прогона тестов сама становится источником уязвимости.

**Прочие исправления.** Параметры стойкости scrypt теперь хранятся вместе с хешем — иначе обновление Node сделало бы непроверяемыми все пароли разом. `/api/health` перестал раскрывать путь к файлу базы. Регистрация получила ограничение частоты. Пароль стенда переехал из кода в `SEED_PASSWORD`. Отдельно найдена и починена потеря каналов связи у клиенток без строки настроек: они молча оставались без напоминания о визите, а оформление записи мастером падало с 500.

**Проверено:** 263 проверки в восьми наборах, включая матрицу «50 адресов × 4 роли» и отдельный набор на пользователя с двумя ролями.

## 15. Готовность к публичному адресу (добавлено 23 августа 2026)

Закрыты три риска высокого приоритета из отчёта безопасности. Все три касались не логики сервиса, а условий, в которых он окажется на сервере.

**TLS.** Сервер умеет поднимать HTTPS сам, если заданы `TLS_KEY_FILE` и `TLS_CERT_FILE`. Заголовок `Strict-Transport-Security` ставится **только на защищённом соединении** — на открытом он бессмыслен, а браузер запомнил бы правило и для локальной разработки. Главное: в рабочем режиме сервер **отказывается стартовать** по открытому HTTP, если TLS не завершает ни он сам, ни доверенный прокси. Обойти можно явным `ALLOW_INSECURE_HTTP=true`, и текст отказа прямо говорит, чем это грозит.

Отказ стартовать — сознательно резкое решение. Забытый TLS не проявляет себя ничем: сервис работает, пока кто-нибудь не прочитает токен сеанса из чужого Wi-Fi. Лучше упасть при развёртывании.

**Адрес клиента за прокси.** `X-Forwarded-For` читается только от адресов из `TRUST_PROXY`. Заголовок от кого угодно другого игнорируется — иначе его подставит любой и обойдёт ограничение по адресу. Цепочка разбирается справа налево: крайний справа добавлен ближайшим прокси и достовернее всего, свои прокси пропускаются, первый недоверенный и есть клиент. Проверено в обе стороны: без `TRUST_PROXY` подставной заголовок игнорируется, с ним — два клиента за одним прокси считаются раздельно.

**Счётчики попыток в базе.** Таблица `rate_limits`, миграция 008. Раньше счётчики жили в памяти процесса: достаточно было дождаться развёртывания, чтобы перебор паролей начался заново. Проверено прямым перезапуском сервера — блокировка сохраняется. Пороги вынесены в окружение, ноль отключает ограничение.

**Ошибка в самой проверке, которую стоит запомнить.** Первая версия теста перезапускала сервер командой «убить все процессы node» — и убивала сама себя, потому что тест тоже работает под node. Разделил на две фазы с перезапуском снаружи.

**Проверено:** 9 отдельных проверок мер защиты плюс полная регрессия — 263 проверки в восьми наборах.

## 16. Критичные находки отчёта (добавлено 23 августа 2026)

Оставшиеся после проверки безопасности замечания разобраны по трём уровням: критичные — те, что дают путь к чужим данным или откроют его после ближайшей правки; важные, но не срочные — те, что усложняют жизнь злоумышленнику, а не закрывают дверь; необязательные — удобство сопровождения. Критичных оказалось три, и они исправлены.

**1. Имена столбцов собирались в текст запроса вручную.** Три места — профиль, настройки студии, переключатели уведомлений — строили `UPDATE … SET имя = $имя` из ключей объекта. Инъекции там не было: ключи брались из литеральных списков в коде. Опасность в другом — это место устроено так, что одна невнимательная правка («давайте просто передадим сюда тело запроса») превращает его в инъекцию, и ни тесты, ни обзор кода этого не заметят.

Появилась `setClause(updates, allowed)` в `db.js`: имя, которого нет в списке разрешённых, до SQL не доходит, а вторая проверка требует, чтобы имя вообще выглядело как имя столбца. Побочная польза — в профиле теперь видно списком, что менять можно только имя, почту, фото и телеграм: телефона и роли в списке нет.

**2. Сеанс жил тридцать дней у всех одинаково.** Украденный токен администратора работал бы месяц. Срок стал зависеть от прав: сутки для администратора и мастера, семь дней для клиентки, с продлением при активности. Продление пишется в базу только после того, как истекла половина срока, — иначе каждый запрос превращался бы в запись.

Разница в сроках не произвол: у сотрудников в руках чужие персональные данные и деньги студии, а входят они с рабочего места и каждый день. Клиентку же выкидывать из приложения раз в сутки — верный способ отправить её записываться в мессенджер.

**3. На ответах не было заголовков безопасности.** `X-Content-Type-Options`, `X-Frame-Options`, `Referrer-Policy` и строгий `Content-Security-Policy` ставятся теперь на каждый ответ — включая ошибки, календарную ленту и предварительный запрос браузера. Отдельно важен `Referrer-Policy: no-referrer`: в ссылке отмены записи есть токен, и без этого заголовка он уехал бы в чужой `Referer` при первом же переходе со страницы.

**Проверено:** 20 отдельных проверок трёх исправлений на живом сервере — от «столбец не из списка до SQL не доходит» до «срок сеанса продлевается с часа до семи дней». Полная регрессия — 263 проверки в семи наборах, аудит прав (50 сочетаний «адрес × роль», утечек нет), 9 проверок мер защиты. Отдельно пройден чек-лист из десяти вопросов: десять «да».

**Находка в самой проверке.** Автоматическая сверка «все ли запросы параметризованы» сначала ответила «нет» и показала одиннадцать мест. Восемь из них — подстановка постоянной части запроса (`SELECT_APPOINTMENT`), объявленной литералом в модуле. Проверка была слишком грубой: она искала подстановку вообще, а опасна подстановка **значения**. Разбор уточнён — теперь он различает, что именно подставляется.

**Что осталось в отчёте.** Важные, но не срочные: раскрытие занятости телефона при регистрации, удержание слота без входа, открытый токен устройства для пушей. Необязательные: журнал действий администратора, шифрование резервных копий. Все они описаны в `security-review.local.md` вместе с формулировкой, как исправлять.

## 17. Черновой фронтенд (добавлено 23 августа 2026)

Папка `web/`: шесть страниц на обычных формах и таблицах — вход и регистрация, услуги и мастера, свободное время, создание записи, кабинет, панель студии. Дизайна нет намеренно: страницы нужны, чтобы проверять API руками, а оформление живёт в прототипе `site/`, который этот код не трогает.

**Свой маленький сервер вместо открытия файлов.** Страница, открытая как `file://`, не может обращаться к API: браузер считает такой запрос межсайтовым. Поэтому `web/serve.js` отдаёт файлы и **переправляет всё, что начинается с `/api`, на бэкенд**. Браузер видит один источник — не понадобилось ни трогать `CORS_ORIGINS`, ни ослаблять заголовки безопасности API ради страниц.

**Ошибки — на странице, а не в консоли.** Любой отказ показывается текстом: код, человеческий заголовок и сообщение сервера. Два случая разобраны особо. Отказ 409 «время занято» приходит с подсказкой ближайших свободных окон — они выводятся кнопками, нажатие подставляет время в форму. Администратору к тому же добавляется строка о галочке «поверх занятого времени»: сервер отвечает ему на языке API (`передайте allow_overlap: true`), а на странице это должно превращаться в действие.

**Права определяются сервером, а не полем роли.** В ответе API приходит одна — основная — роль, а прав может быть больше: владелица студии одновременно администратор и мастер. Первая версия страницы решала по этому полю и прятала от владелицы панель студии. Исправлено так: профиль мастера в базе бывает только у пользователя с основной ролью `master`, значит администратор мастером быть не может, а мастер может оказаться администратором — и это проверяется одним запросом к административному адресу. Тому, кто им не является, страница показывает только своё расписание.

**Время и деньги.** Поле ввода ждёт ISO-8601 UTC — то, что API и отдаёт; рядом показывается то же время по часовому поясу студии. Так на странице не появляется собственной арифметики с поясами: значения либо приходят от сервера, либо подставляются нажатием на свободное окно. Копейки переводятся в рубли только при выводе.

**Проверено вживую** в браузере: вход клиентки, администратора, мастера и владелицы; каталог; расчёт окон (включая «в этот день мастер не работает»); удержание окна и запись; повторная запись на занятое время с подсказкой окон; перенос и отмена; повторная отмена (409); панель с фильтрами, подтверждение и отмена записи; оформление поверх занятого времени; правка и удаление услуги (409 «по услуге есть записи»); правка и удаление мастера (409). Ни одна страница не завершилась ошибкой в консоли — единственный 403 в журнале сети это та самая проверка прав.

## 18. Инструкция запуска и то, что она вскрыла (добавлено 23 августа 2026)

`README.md` переписан так, чтобы человек, впервые увидевший проект и не имеющий ничего установленного, дошёл до работающего сервиса без вопросов: что поставить (только Node 22.13+ и Git), пять шагов запуска, учётные записи стенда, команды, настройки, разбор частых неприятностей, ссылки на документы. Отдельно сказано, чего делать **не** нужно: ставить СУБД и выполнять `npm install` — зависимостей у проекта нет.

**Инструкция проверена не глазами, а руками.** Репозиторий склонирован в отдельную папку и пройден по собственным шагам с нуля. Это дало три находки, две из которых — настоящие дефекты.

**1. Пустое значение в `.env` отменяло умолчание.** Шаг «скопируйте `.env.example` в `.env`» приводил к файлу, где все строки пустые. Запись вида `Number(process.env.PORT ?? 3000)` в этом случае даёт не 3000, а ноль: `??` подставляет умолчание только вместо `undefined` и `null`, а из файла приходит пустая строка. Сервер послушно занимал случайный порт и печатал «слушает http://127.0.0.1:0».

Там же, но хуже: пороги ограничения частоты. Пустая строка превращалась в ноль, а ноль по нашему же соглашению означает «ограничение выключено». **Копирование образца молча снимало защиту от перебора паролей.** Пустой `SEED_PASSWORD` заводил учётные записи стенда с пустым паролем, войти под которыми потом нельзя — валидация такой пароль не принимает.

Появился `server/src/env.js`: пустая строка и строка из пробелов считаются «не задано», нечисловое или отрицательное значение отбрасывается с предупреждением в лог. Подключён везде, где это важно: порт, путь к базе, пароль стенда, пороги ограничения частоты, срок жизни сеанса. Явный ноль по-прежнему выключает ограничение — это осознанная настройка, а не оставленная пустой строка.

**2. Занятый порт объяснялся стеком вызовов.** Самая частая неприятность первого запуска — на 3000 уже что-то работает — встречала новичка десятком строк из внутренностей Node. Теперь сервер говорит, что порт занят, и показывает две команды: как запустить на другом порту в bash и в PowerShell. Это та же линия, что и в `env-check.js`: падать сразу и внятно.

**3. Черновой фронтенд в репозиторий ещё не отправлен.** В свежем клоне нет ни папки `web/`, ни команды `npm run web` — они лежат только в рабочей копии. Шаг «откройте страницы» у нового человека не сработает, пока изменения не закоммичены. Это не дефект кода, а состояние репозитория, но для инструкции разница невелика.

**Проверено после правок:** полная регрессия на пересозданной базе — 263 проверки в семи наборах, аудит прав (утечек нет), 9 проверок мер защиты, 20 проверок критичных исправлений, чек-лист из десяти вопросов, плюс 8 отдельных проверок чтения окружения: пустое значение, заданное значение, явный ноль и мусор вместо числа.

**Замеченное про сам стенд.** Наборы проверок переименовывали учётные записи (`PATCH /api/profile`, `PATCH /api/admin/masters`), и наборы, которые ищут мастера по имени «Лена», переставали его находить. Это не дефект сервиса, а накопленный дрейф данных: после `npm run reset` всё зелёное. Вывод на будущее: проверки, меняющие общие данные стенда, должны возвращать их в исходное состояние или работать на своих.

## 19. Молчащий экран (добавлено 29 августа 2026)

Правило простое: любой ответ сервера показываем текстом на экране, особенно отказ. Молчащий экран — не мелкая недоделка, а единственное, что видит человек, прежде чем уйти. Проверка показала, что правило соблюдалось наполовину.

**Что было.** Обёртка `guard` печатала ошибку на страницу, и всё, что запускается по нажатию, было закрыто — 21 вызов. А запросы первой загрузки стояли на верхнем уровне модуля, вне обёртки. Проверка простая: остановить бэкенд и открыть страницы. Пять из шести молчали — «Загрузка…» навсегда, поле сообщений пустое, а понятный текст «Бэкенд не отвечает» лежал в консоли, куда клиент не смотрит.

**Что сделано.** Загрузка каждой страницы переведена на ту же обёртку, что и нажатия. Сверх того в `app.js` появилась общая сеть — обработчики `unhandledrejection` и `error`: даже вызов, написанный завтра мимо обёртки, всё равно покажет отказ на экране. Заодно надпись «Загрузка…» после ошибки заменяется на «не загрузилось — см. сообщение выше»: сообщать, что данные едут, когда они уже не едут, — тот же обман, только вежливый.

**Два дефекта, найденных по дороге.**

Первый — неудачный вход выбрасывал того, кто уже вошёл. Обёртка над запросами считала любой ответ 401 протухшим сеансом: стирала пропуск и писала «Сеанс закончился». Но на входе и регистрации 401 означает «неверный пароль». Теперь эти два адреса из правила исключены, и экран показывает то, что ответил сервер. Найдено не разбором кода, а руками в браузере: ввёл неверный пароль под действующим сеансом и остался без сеанса.

Второй — регрессия от самой правки: страница записи стала падать на часовом поясе студии, которого при первой отрисовке ещё нет. Показательно, что поймала её новая сеть — вместо молчания на экране появился текст ошибки. Починено, но вывод тот же, что и в разделе 14: правка без немедленной проверки в браузере сама создаёт поломку.

**Проверено вживую**, кнопками, полным циклом: регистрация без согласия (400 красным) и с согласием, вход с неверным и верным паролем, каталог, расчёт окон, удержание, запись, повторная запись на то же время (409 с пятью подсказанными окнами, нажатие подставляет время), перенос, отмена, закрытая для клиентки панель студии, работа панели под администратором с пересчётом сводки. Затем бэкенд остановлен повторно — все шесть страниц показали отказ текстом.

## 20. Первые семь экранов клиента (добавлено 29 августа 2026)

Лендинг, вход, регистрация, восстановление доступа, смена пароля, устройства и входы, кабинет. Плюс общая шапка, которая собирается один раз и переиспользуется. Черновой стенд предыдущего этапа переехал в `web/draft/` — он остаётся полезным для адресов, у которых чистового экрана пока нет.

**Два решения на стороне сервера, без которых интерфейс не собирался.**

*Один порт вместо двух.* Раньше страницы отдавал отдельный сервер на 5173 и переправлял `/api` на 3000. Теперь всё отдаёт один процесс: `npm start`, `localhost:3000`. Причина не в удобстве запуска, а в пропуске — он лежит в куке, а кука привязана к источнику: при разных портах браузер просто не пришлёт её вместе с запросом. Отдельный `web/serve.js` удалён, команда `npm run web` убрана.

*Пропуск переехал из localStorage в куку.* Сервер ставит её сам при входе и регистрации: `HttpOnly`, `SameSite=Lax`, срок — тот же, что у сеанса. Разница не косметическая: `localStorage` читается одной строкой скрипта, кука с `HttpOnly` — нет. Интерфейс токен больше не хранит и не видит вовсе. Заголовок `Authorization` продолжает работать — им ходят скрипты проверки и внешние клиенты, у которых куки нет.

**Строгий CSP заставил переписать структуру страниц — и правильно сделал.** Ответам API оставлен прежний запрет на всё (`default-src 'none'`), а страницам выдана своя политика: только собственные файлы плюс шрифты Google. Первая версия лендинга держала код в `<script type="module">` прямо в HTML — браузер отказался его выполнять. Вместо послабления политики код экранов уехал в `web/js/<экран>.js`, оформление — в `web/css/`. Инлайновых стилей и скриптов в проекте теперь нет ни одного.

**Что взято из прототипа один в один, а что собрано иначе** — подробно в отчёте к этому шагу; коротко: структура, порядок блоков и тексты лендинга взяты из прототипа, а списки услуг, мастеров, работ, часы работы и контакты подтянуты из API. Рейтинг студии в первом экране считается на клиенте как среднее по мастерам, взвешенное по числу отзывов: отдельного адреса для него нет (расхождение №6 карты экранов). Фотографий нет — в API есть только адреса файлов, поэтому на их месте рамки-заглушки, как в прототипе.

**Восстановление пароля осталось без формы.** Адреса сброса в API нет, и форма, которая никуда не отправляет, врала бы человеку. Страница честно объясняет, что делать: контакты студии из `GET /api/studio` и смена пароля для тех, кто вошёл.

**Проверено действиями, а не глазами:** вход с верным и неверным паролем, регистрация нового клиента и попытка на занятый телефон, переход между страницами с сохранением сессии, смена пароля с несовпадающим повтором и с верным, список устройств. Local Storage пуст, `document.cookie` для скрипта пуст. Отдельно прогнана регрессия API — 19 проверок, включая права, конфликт слотов и строгий CSP для `/api`.

## 21. Путь записи и кабинет (добавлено 29 августа 2026)

Собран весь клиентский сценарий: выбор услуг → мастер → календарь → подтверждение → успех, плюс кабинет с деталями, отменой и переносом. Семь новых страниц, выбор между шагами хранится в `web/js/store.js`.

**Состояния экрана вместо отдельных страниц.** В прототипе «нет слотов», «слот занят» и «пустой кабинет» нарисованы отдельными файлами — иначе их не показать на картинке. В коде это одна страница, которая ведёт себя по-разному. Экран времени различает три состояния: считаем, есть окна, окон нет — и это именно три разных вида, а не пустая сетка в двух случаях из трёх.

**Занятое время видно и неактивно.** Сервер отдаёт только свободные окна. Прятать занятые нельзя: клиентка, видевшая окно в 15:00 минуту назад, решит, что сервис врёт. Поэтому сетка дня достраивается из рабочих часов студии и шага сетки — обе величины приходят от сервера, — а свободным окно становится только если так сказал сервер. Расчёт остаётся у него, на экране только показ.

**Удержание оказалось не украшением.** Экран подтверждения запрашивает удержание и показывает таймер. Две находки по дороге. Первая: снимать удержание при уходе со страницы через `sendBeacon` нельзя — маячок умеет только POST, а удержание снимается методом DELETE; помог `fetch` с `keepalive`. Вторая: при возврате на экран сервер честно отвечал «время удерживается» — нами же, поэтому перед новым удержанием старое отпускается явно.

**Отказ «время заняли» приходит с альтернативами — но не всегда.** При отказе в записи сервер сам присылает ближайшие окна в `details.available`. При отказе в удержании — нет. Экран в этом случае спрашивает окна обычным запросом к тому же серверу: своих вариантов не придумывает ни в одном из случаев.

**Мелочь, которая стоила бы вечера.** Список мастеров отдаёт имя полем `name`, карточка одного мастера — полем `full_name`. В сводке подтверждения имя мастера просто не появлялось. Поймано глазами на экране, а не тестом.

**Несколько услуг — несколько записей.** Сервер сохраняет по записи на услугу; экран успеха показывает их все и общий итог, а не первую из списка.

**Проверено восемью сценариями** с двумя разными клиентами: запись, исчезновение слота, конфликт из «второго браузера», удержание (второй клиент окна не видит, после снятия оно возвращается), пустой кабинет, отмена с возвратом окна, перенос на занятое время, чужая запись по номеру — 403 без данных. Двадцать пять проверок, все зелёные. Плюс регрессия API и проверка страниц с кукой.

**Одно расхождение, о котором стоит помнить.** Первая ошибка в самих сценариях: окна брались из устаревшего списка. Запись съедает не только своё время, но и соседние точки сетки — на длительность плюс перерыв мастера. Свободные окна надо перезапрашивать после каждой записи.

## 22. Узкий экран и приёмка клиентской части (добавлено 29 августа 2026)

**Адаптивность.** Осмотр на ширине 375 показал ровно то, что и должен был: ломается там, где на широком экране что-то стояло рядом. Логотип и имя в шапке переносились в три строки, шаг-индикатор занимал две, слоты шли по три в ряд кнопками 106×44, ячейки календаря были 38×38, закреплённая полоса накрывала последнюю карточку.

Починено перестроением, а не удалением: меню уехало во вторую строку шапки и прокручивается внутри себя, у шага-индикатора осталась подпись только у текущего шага, слоты стали по два и выросли до 164×52, ячейки календаря — до 44, полоса разложилась в две строки, а содержимое получило отступ по её высоте. Все правки — в блоках `@media (max-width: …)`; настольные правила написаны через `min-width` и потому не затронуты. Ничего не спрятано: набор кнопок на узком и широком экране совпадает.

**Приёмка.** Сверка с прототипом дала четыре расхождения, каждое правилось отдельно: в карточке кабинета не было действий «Перенести» и «Отменить» (в прототипе они прямо в карточке), в истории не было кнопки «Повторить», ссылка «Правила отмены» вела на несобранную страницу, а в поле имени при регистрации стояло имя настоящей клиентки из базы.

Чек-лист приёмки пройден целиком — 23 проверки: сценарий записи, данные из базы во всех файлах, четыре состояния экрана, конфликт с альтернативами и запись со второй попытки, отмена с освобождением окна, чужая запись → 403, ни одного значения мимо токенов, правила узкого экрана при нетронутых настольных, ссылок в никуда нет, в репозитории нет базы и `.env`.

**Уборка черновика.** Пять клиентских черновых страниц удалены — их заменили настоящие экраны. Осталась `draft/admin.html`: чистовой админки ещё нет.

При проверке выяснилось, что черновая страница студии сломалась дважды и молча. Во-первых, она жила на токене в localStorage, а сессия переехала в куку. Во-вторых — и это важнее — её код лежал инлайном в HTML, а страницы теперь отдаются со строгой политикой безопасности, которая инлайн запрещает. Оба раза лечение одно и то же, уже применённое к чистовым экранам: код вынесен в `draft/admin.js`, ширины полей — в `draft/styles.css`, проверка сеанса стала серверной. Вывод на будущее: страница, которую «оставили на потом», ломается тихо — её нужно открывать после каждой смены общих правил.

**Экран профиля** собран последним: данные, напоминания переключателями, календарная лента, любимые мастера, регулярные записи и согласия. Он был единственным экраном прототипа, которого не хватало в клиентской части.

**Журнал подключения** — `docs/frontend-log.md`: двенадцать экранов с адресами и полями, десять поломок с разбором и список того, чего в клиентской части ещё нет.

## 23. Лист ожидания, абонементы, баллы и служебные страницы (добавлено 29 августа 2026)

Закрыт список «чего ещё нет» из журнала подключения — кроме экранов студии, они следующей итерацией.

**Лист ожидания.** Кнопка появляется ровно там, где она нужна: на экране времени в состоянии «окон нет». Свои заявки — вкладкой в кабинете. Повторная заявка на тот же день отвечает 409, и это не ошибка, а «вы уже стоите» — показано спокойным сообщением, а не красным.

**Абонементы и баллы** — страница `/bonuses`. Визиты абонемента показаны отметками, а не полосой в процентах: у абонемента считанное число визитов, «три из пяти» читается лучше шкалы. Заодно обошлись без инлайновых стилей, которые запрещает политика страниц, — первая версия задавала ширину полосы через `style` и была бы заблокирована.

**Ни одной кнопки, которая обещает невозможное.** Абонемент продаёт студия, баллы списывает касса — клиентке сервер отвечает «Списывает баллы студия». Экран это и пишет вместо неработающей кнопки. Проверено: попытка списать свои баллы даёт 403, чужой абонемент по подстановке `client_id` не отдаётся.

**Служебные страницы.** 404 отдаёт сам сервер вместо любого несуществующего адреса — страницей с кодом 404, при этом `/api/*` по-прежнему отвечает JSON. Страница «сервис не отвечает» сама проверяет связь: с остановленным сервером она честно пишет «Связи по-прежнему нет», с поднятым — предлагает вернуться. Проверено обоими состояниями.

## 24. Каркас админ-панели и разграничение доступа (добавлено 4 сентября 2026)

Первый экран, закрытый ролью. До сих пор сервис различал только «вошёл» и «не вошёл», а владение проверял по записям клиентки. Административные адреса существовали с 23 августа, но открыть их было неоткуда: расписание правили в базе, услуги — миграцией.

**Три проверки, а не одна.** Вход, роль и владение — разные вопросы, и отвечать на них нужно по отдельности. Гость и клиентка получают на `/admin` разные ответы: первого уводит на вход, второй показывается страница отказа. Слить их в один «нет доступа» было бы проще, но гость не сделал ничего плохого — ему нужно предложить войти, а не сообщить, что он не тот.

Владение у администратора не отключено, а работает по другому правилу: `GET /api/appointments/my` отдаёт свои записи и отвечает 403 на чужой номер, `GET /api/admin/appointments` отдаёт все. Записано это в самом адресе, а не во флаге внутри обработчика.

**Проверка прав вынесена в одно место.** В маршрутизаторе появился `router.guard(prefix, check)`: проверка срабатывает до обработчика на всех адресах с указанным префиксом. Весь `/api/admin/` закрыт одной строкой в `index.js`.

Сделано это ради кода, которого ещё нет. `requireRole` в тридцати девяти обработчиках — не защита, а тридцать девять мест, где можно забыть про сороковое. Проверено отдельно: адрес `/api/admin/*` с обработчиком, в котором нет ни одной проверки прав, отвечает клиенту 403. Сами вызовы `requireRole` из обработчиков не убраны — им нужен не допуск, а сам пользователь: кто завёл услугу, кто подтвердил запись.

**Страницу закрывает сервер, а не спрятанная ссылка.** Проверка стоит в `static.js`, до отдачи файла. Если закрыть только запросы к данным, клиентка откроет `/admin` и увидит каркас панели с пустыми таблицами: данных не утечёт, но человек решит, что панель сломалась, а не что она не для него. Пункт меню от клиентки скрыт — это удобство, и полагаться на него нельзя.

**Роли поехали в интерфейс.** `POST /api/auth/login` и `GET /api/auth/me` теперь отдают не только основную роль `role`, но и полный список `roles`. Без него страница не могла решить, вести человека в кабинет или в панель: у владелицы студии по замыслу две роли, и по одному полю её от обычного мастера не отличить. Список ничего не разрешает — права сервер по-прежнему проверяет сам, по базе.

Правило «ролей может быть несколько» проверено на живом сервисе: мастеру временно выдана вторая роль `admin` — панель открылась, роль снята — снова 403. Сравнение `role === 'admin'` такого человека не пропустило бы.

**Вход остался один.** Отдельной формы для сотрудников нет: вторая форма не добавила бы защиты, зато восстановление и смену пароля пришлось бы поддерживать в двух местах. Куда идти после входа, решают роли из базы. Заодно закрыт открытый редирект: адрес возврата `?next=//чужой-сайт` начинается со слеша и выглядел своим — теперь пропускается только один слеш.

**Чего в этом заходе нет.** Страницы разделов пустые: каркас, меню и защита. Данные — следующий шаг. Расписание и настройки пока живут только в карте экранов.

**Демо-данные показывали обратное тому, что написано в миграции.** Владелица `anna@nogotochkee.ru` была заведена только мастером, а администратором — отдельная учётная запись. Ровно то разделение, ради отмены которого таблица `user_roles` и появилась: у владелицы, которая сама принимает клиенток, визиты выпадали бы из расписания и отчётов. Теперь сиды выдают ей вторую роль списком, основная остаётся `master` — на неё опирается внешний ключ `master_profiles`. Сценарий с двумя ролями проверяется обычным входом, а не правкой базы руками.

**Клиентские экраны сотруднику отказывали красным.** Кабинет запрашивает `GET /api/appointments/my` с ролью `client`, страница абонементов — `GET /api/passes` и `GET /api/loyalty`, которые от сотрудника ждут `client_id`. Ссылки в шапке при этом предлагались всем вошедшим: администратор нажимал «Мои записи» и получал отказ, исправить который не мог.

Правило сервера `actsAsClient` повторено в `web/js/api.js`, и клиентские пункты меню показываются только тем, кому они откроются. Сами страницы, если адрес набрали руками, объясняют это словами и уводят в панель: отказ должен быть понятным, а не красным (правило 7).

## 25. Услуги и мастера правятся из панели (добавлено 4 сентября 2026)

Справочники переехали из миграций в интерфейс. Прайс и состав мастеров теперь меняет владелица, а не разработчик.

**Что произойдёт по кнопке «Удалить», решает сервер.** Раньше он отвечал 409 с советом «снимите с публикации: PATCH is_active=false» — это ответ программисту, а не человеку. Администратор не обязан помнить, есть ли по услуге записи, поэтому проверку делает сервер, и оба исхода успешные: строку без ссылок он удаляет, строку с историей отключает и объясняет, чем именно она держится — записями, регулярными сериями, абонементами.

Считаются все три таблицы с `ON DELETE RESTRICT`, а не только записи. Если ссылка найдётся там, где не считали, попытка удаления упрётся во внешний ключ — тогда услуга тоже отключается, просто без подробностей в сообщении. Ошибку базы человеку не показываем ни в одном из случаев.

**Схема базы не менялась.** Проверили первым делом: `appointments` с самого начала хранит `duration_min` и `price_kopecks` собственными полями, а не смотрит в справочник — в схеме они прямо помечены как снимок. Миграция не понадобилась, копирование при оформлении уже работало. Проверено на живом сервисе: цена услуги поднята с 1 500 до 9 990 ₽, в записи, оформленной до правки, осталось 1 500; новая запись берёт новую цену.

Чего снимка нет — названия. Переименование услуги меняет подпись и в старых записях. Для отключения это неважно (имя остаётся прежним), но переименование историю перепишет. Отдельного поля под это заводить не стали: паспорт продукта такого не требует, а лишняя колонка живёт вечно.

**Отметки услуг у мастера — это и есть клиентский выбор.** Отдельной настройки «кого показывать» нет: `GET /api/masters?service_id=…` отбирает по `master_services`. Снятая галочка на экране мастера убирает его из выбора при записи на эту услугу — проверено кругом через интерфейс.

Список мастеров стал отдавать `service_ids`. Клиентский `GET /api/masters/:id` для формы правки не годился: он не показывает услуги, снятые с публикации, а за мастером они всё равно закреплены — экран терял бы отметки при сохранении.

**Услуга без мастера — тихая ловушка.** Она видна в прайсе, но записаться на неё не к кому, и клиент узнаёт об этом только на втором шаге. В списке услуг такая строка помечена «Нет мастера», в списке мастеров пустой набор помечен «Нет услуг».

**Что нашли по дороге.** Атрибут `hidden` не работал: браузер прячет им элемент единственным способом — `display:none`, а любое своё правило с `display` это перебивает. Поле пароля в форме мастера оставалось на экране при правке, кнопки «Отмена» и «Удалить» — в режиме создания. На этом уже спотыкались с окном подтверждения (в `booking.css` так и осталось точечное `.modal[hidden]`), теперь в `base.css` есть общее правило.

Панель проверили на 375 пикселях: боковое меню занимало весь первый экран, а название услуги в карточке переносилось по одному слову в строку. На узком экране меню стало полосой сверху, карточка складывается в столбик.

**Цена на форме в рублях, в запросе в копейках.** Классическая ошибка «вместо 2 500 ₽ показано 250000» не воспроизводится: ввод умножается на сто, вывод делится.

## 26. Расписание, чужие записи и уведомления (добавлено 4 сентября 2026)

Панель собрана целиком. Экран «Записи» — единственное место сервиса, где один человек меняет планы другого, поэтому правило здесь одно: у каждого действия должна остаться причина, след и понятный результат для того, кого оно касается.

### Что администратор может сделать с чужой записью и что после этого видит клиентка

| Действие | Что в базе | Что видит клиентка |
|---|---|---|
| **Подтвердить** | статус `pending` → `confirmed`, строка в `appointment_status_log` | визит в кабинете становится подтверждённым; уведомления нет — для неё ничего не изменилось, она и так ждала этого визита |
| **Отменить** | статус → `cancelled`, в журнал попадают причина и кто отменил. Запись не удаляется, время освобождается, напоминания снимаются, освободившееся окно предлагается листу ожидания | запись остаётся в кабинете со статусом «отменена» — не исчезает. В уведомлениях: «Визит пятница, 11 сентября, 19:00 отменён студией. Причина: мастер заболел» |
| **Перенести** | `starts_at` меняется у той же записи, в журнал пишется «перенос с … на …». Новой строки не появляется, напоминания пересобираются от нового времени | в кабинете то же посещение с новым временем. Одно уведомление: «Запись на пятница, 11 сентября, 10:00 перенесена на пятница, 11 сентября, 11:00» |
| **Записать поверх занятого** | новая запись с `allow_overlap = 1`. Проверка пересечения срабатывает как обычно и пропускает только этот признак | своя запись не меняется. Уведомление: «Студия назначила ещё один визит на ваше время: … Ваша запись сохранена» |
| **Заблокировать время** | строка в `time_off`; мастер остаётся активным | заблокированные часы просто не предлагаются при записи. Уже назначенные визиты остаются — их переносят вручную, и об этом администратору говорится прямо в ответе |

### Уведомления в кабинете

Новая таблица `inbox` (миграция `009_inbox.sql`). Класть их в существующую `notifications` нельзя: та таблица — очередь отправки наружу, со сроком, каналом и отметкой о доставке. Здесь другое: список, который человек открывает и просматривает, и признак прочтения принадлежит ему, а не отправщику.

Создаются в одном месте — `notifyClient` в `services/inbox.js`, и там же живёт правило «не сам»: если событие вызвал сам получатель, сообщение не создаётся. Проверка стоит внутри функции, а не в трёх местах вызова, — иначе четвёртый вызов однажды забудут.

Поводов ровно три: отмена студией, перенос студией, чужая запись поверх её времени. Напоминания о визите сюда не добавлены сознательно: им нужен запуск по расписанию, а это отдельный механизм и отдельное решение о частоте.

Текст хранится готовым, а не собирается при показе. Сообщение описывает то, что было в момент события: если запись потом перенесут ещё раз, старое уведомление должно остаться правдой о прошлом переносе.

### Наложение остаётся защитой, а не отключается

Проверка пересечения не снимается для административных запросов. Сервер отвечает 409 и присылает ближайшие свободные окна; поверх занятого времени запись создаётся только вторым, отдельным действием — с `allow_overlap: true`. Двойной щелчок или ошибка в интерфейсе второй визит не создадут.

Наложение в списке отмечается по времени, а не по признаку `allow_overlap`: признак стоит на новой записи, а пометить нужно обе — и ту, поверх которой пришли.

### Сутки студии, а не сутки по Гринвичу

`GET /api/admin/appointments?date=` раньше сравнивал первые десять символов `starts_at` — это ответ на вопрос «что сегодня по Гринвичу». Теперь день переводится в диапазон UTC по часовому поясу студии (`localDayRangeUtc` в `time.js`). Для Москвы разница видна только у визитов до трёх ночи, но правило «время считаем и показываем по поясу студии» не должно иметь исключений.

### Что проверено

Тридцать шесть проверок по живому сервису: список дня и его границы, совпадение времени в панели и в кабинете, перенос без появления второй записи, освобождение прежнего окна, отмена со следом в журнале и статусом в кабинете, отказ 409 с альтернативами, создание поверх по подтверждению, недоступность того же клиенту, блокировка без отключения мастера, доступность мастера на другие даты, три уведомления с текстами и ссылками, счётчик до и после прочтения, отсутствие уведомления при собственном действии, невидимость чужих сообщений.

### Что нашли по дороге

Слово `null` посреди карточки визита. `replaceChildren` не пропускает `null` мимо, как это делает наш `el`, — он превращает его в текстовый узел. Конструкция «условие ? элемент : null» безопасна только внутри `el`. Заведён `fill` в `api.js` и применён в четырёх местах, где на верхний уровень мог прийти `null`: карточка визита, контакты в подвале лендинга, блок студии и согласия в профиле.

## 27. Вход через внешний сервис (добавлено 4 сентября 2026)

Собрана наша половина входа через Яндекс. Настоящего Яндекса в ней пока нет и быть не может: он должен вернуть человека обратно на сайт, а адрес возврата обязан совпадать до протокола, домена и порта — у сервиса на `localhost` такого адреса нет. Вместо обращения наружу стоит заглушка.

### ✓ Закрыто 9 сентября 2026: заглушки больше нет

**Вопрос снят.** Заглушка удалена из кода вместе с переменной `YANDEX_LOGIN_STUB`, на её месте работает настоящий Яндекс — см. раздел 40. Того, чего нет, нельзя включить по забывчивости.

Ниже — как это было записано, пока заглушка жила в проекте, и почему предупреждение стояло здесь:

> `YANDEX_LOGIN_STUB` должен остаться пустым на сервере. Пока он `true`, войти одним нажатием сможет любой, кто откроет сервис: пароль не спрашивается, проверки со стороны Яндекса нет. По умолчанию выключено, включается только переменной окружения — в коде для этого ничего править не нужно, и это главное: временное решение опасно не тем, что оно временное, а тем, что о нём забывают.

### Что разделено и зачем

`services/external-login.js` состоит из двух половин. `yandexProfile` — единственное, что придётся переписать после публикации: два запроса к Яндексу вместо чтения настроек. `linkOrCreate` — то, что работает по-настоящему уже сейчас и не изменится: поиск человека, привязка входа, роль, заготовки профиля.

### Почту сервер от браузера не принимает

Адрес принимает одноразовый код, а не почту. Обработчик, который поверил бы браузеру на слово, был бы не входом через Яндекс, а входом под кем угодно: достаточно прислать чужую почту. Проверено запросом с `{"email": "admin@nogotochkee.ru", "role": "admin"}` в теле — сервер завёл клиентку с почтой из заглушки и в панель её не пустил.

### Порядок поиска

Сначала по паре «сервис + идентификатор»: почта у человека меняется, идентификатор в Яндексе — нет. Затем по почте: если человек уже регистрировался с паролем, второй пустой кабинет ему заводить нельзя — это тот же человек, просто вошёл иначе. Роли при привязке не трогаются, имя из Яндекса своё не затирает, пароль остаётся.

Новая учётная запись всегда клиентка. В уроке эта роль называется `user`, у нас роль обычного пользователя называется `client` — это она. Значение записано в запросе буквально, а не берётся из ответа сервиса: администратора или мастера через внешний вход выдать нельзя ни при каких данных.

### Токен Яндекса нигде не хранится

Он подтверждает одно: человек владеет учётной записью в Яндексе. О правах внутри нашего продукта не говорит ничего. После получения почты выбрасывается, пропуск выдаётся свой — тот же, что и при обычном входе.

### Что поменялось в базе

Миграция `010_external_login.sql`: поля `provider` и `provider_id`, уникальный индекс на пару, требование «у клиентки есть телефон» стало «телефон или внешняя учётная запись». Пароль трогать не понадобилось — он и так был необязательным.

Пересобрать `users` оказалось непросто. Изменить `CHECK` у столбца SQLite не умеет, таблицу нужно создавать заново, а на `users` ссылаются двадцать три таблицы. `DROP TABLE` при включённой проверке внешних ключей ведёт себя как `DELETE FROM`: тянет каскады и упирается в `ON DELETE RESTRICT`. Выключить проверку изнутри транзакции нельзя — `PRAGMA foreign_keys` там молча ничего не делает; `defer_foreign_keys` тоже не спасает, потому что каскады срабатывают всё равно. Первая попытка честно откатилась целиком.

Поэтому у прогона миграций появился особый режим: отметка «ПЕРЕСБОРКА ТАБЛИЦЫ» в начале файла. Проверка выключается снаружи транзакции, внутри переливаются данные, а перед фиксацией выполняется `PRAGMA foreign_key_check` — если хоть одна ссылка ведёт в никуда, всё откатывается. Выключенный на время внешний ключ не должен превращаться в способ тихо оставить мусор в базе.

### Пароль для того, у кого его нет

Человек, вошедший через Яндекс, не может сменить пароль: его нет, а форма требует текущий. Теперь при пустом `password_hash` первый пароль заводится без «текущего» — человек уже вошёл, пропуск это и подтверждает. Так у него появляется второй способ входа, не отменяющий первый.

Страница восстановления доступа получила раздел «Если вы входили через Яндекс»: пароля нет, сбрасывать нечего, войдите тем же способом. Проверять почту на наличие пароля страница не стала бы при всём желании — такой ответ говорил бы постороннему, заведён ли в сервисе кабинет на этот адрес.

### Что проверено

Двадцать шесть проверок с перезапуском сервера под разные настройки: отказ при выключенной заглушке и отсутствие кнопки, первый вход с новой почтой, поля в базе, единственная роль `client`, повторный вход без второй строки, привязка к кабинету с паролем и сохранность его записей, неизменность ролей, работоспособность обычного входа, игнорирование роли и почты из тела запроса, первый пароль без «текущего».

## 28. Чек-лист ручной проверки (добавлено 4 сентября 2026)

Собран `docs/test-checklist.md` — 73 сценария для прохода сервиса целиком руками. Заполняется человеком в браузере: у каждого сценария пустые поля «Увидели» и «Статус».

**Откуда сценарии.** Три источника, как и задумано. Из паспорта продукта — пять сценариев использования и раздел «Ограничения и риски»: запись без регистрации, повтор из кабинета, две клиентки на одно окно, ручное подтверждение, отпуск мастера. Из журнала разработки и журнала подключения экранов — всё, что однажды чинили: молчащая первая загрузка, неудачный вход поверх живого сеанса, инлайновый код против строгой политики страниц, неснятое удержание, возврат на подтверждение, имя мастера в сводке, экран успеха при нескольких услугах, панель поверх карточки на узком экране, ссылка в никуда. Такие сценарии помечены в файле как регресс с указанием находки. Остальное — из кода: места, где проверка прав, часовой пояс или состояние записи могли разойтись с ожиданием.

**Разделено по ролям**, потому что один и тот же экран для гостя, клиентки и администратора работает по-разному: 10 сценариев гостя, 27 клиентки, 20 администратора, 11 про безопасность и 5 второстепенных отдельным списком. Десять сценариев требуют двух одновременных сеансов — обычное окно и окно в режиме инкогнито; в двух обычных вкладках пропуск один и тот же, и проверить конфликт слотов или доступ к чужому не получится.

**Половина сценариев — про то, как что-то идёт не так.** Основной путь проходили при сборке каждого экрана, ошибки прячутся за его пределами: неверный пароль, пустая форма, чужой номер записи в адресе, отказ от отмены посреди подтверждения, повторная заявка в лист ожидания, адрес возврата на чужой домен, перебор пароля.

**Ожидаемый результат везде сформулирован так, чтобы его было видно на экране.** Не «данные сохраняются корректно», а «в кабинете та же запись с новым временем, второй не появилось, прежнее окно освободилось».

**Результаты не заполнены сознательно.** Анализ кода не заменяет проход руками: неверное время, пустой список, съехавшую вёрстку и непонятное сообщение видно только в браузере. Отдельно отмечено правило прогона: ничего не чинить по дороге, иначе результаты уже пройденных сценариев начинают относиться к прошлой версии сервиса.

В конце файла — пустая таблица «Находки». В неё после прогона переносятся сценарии со статусом «не пройдено»; она станет планом работы на следующий шаг.

## 29. Запись без регистрации (добавлено 4 сентября 2026)

Находка Н-1 прогона чек-листа: сценарий Г-5 не проходил. Гость доходил до подтверждения и упирался в кнопку «Войти», прямой запрос отвечал 401 «Требуется вход». Паспорт продукта описывает это как функцию 1 и сценарий использования 1: «записаться за минуту, не заводя ещё один аккаунт».

**Причина.** `POST /api/appointments` начинался с `requireRole(req, 'client')`, то есть требовал вошедшего. Никакой поломки не было — ветку для гостя просто не собрали, когда делали клиентскую часть.

**Что сделано.** У адреса появилась вторая ветка: если пропуска нет, сервер ждёт имя, телефон и согласие на обработку данных, находит человека по телефону или заводит нового — с пустым паролем, как «гостью, которую записала студия». Дальше запись создаётся тем же `createAppointment`, что и у вошедшей клиентки: одна проверка пересечений, одна транзакция, один журнал.

Три решения, которые стоит назвать вслух:

- **Сеанс не выдаётся.** Записался — остался гостем. Пропуск по одному номеру телефона пускал бы в чужой кабинет всякого, кто этот номер знает.
- **Роль всегда клиентская**, что бы ни лежало в базе у этого телефона. Иначе номер мастера или администратора давал бы права по форме без пароля: оформление задним числом и запись поверх занятого времени. Проверено запросом с телефоном администратора и `allow_overlap: true` — визит создан обычным, `source: site`, наложения нет.
- **Имя существующего человека не переписывается.** В форме мог оказаться кто угодно, а в базе — то, как студия его знает.

**Чего это не решает.** Посторонний может оформить визит на чужой номер. Это свойство любой записи без регистрации; защищает от него не сервис, а звонок студии. Настоящая защита — код подтверждения в Telegram или SMS, канала для этого пока нет. Ограничение частоты по адресу стоит: перебирать номера пачками не выйдет.

**Экран «Готово» у гостя.** Вошедшая клиентка перезапрашивает визит у сервера — показываем то, что действительно сохранено. Гостю сервер этого не отдаст, он его не узнаёт, поэтому у него на руках ответ сервера на создание записи. Это тот же сохранённый визит, просто без второго обращения.

**Проверено:** 15 проверок. Запись гостя, отсутствие пропуска, пустой пароль и записанное согласие, повторный визит того же телефона без второй строки, отказы без согласия и без телефона, запись по телефону администратора без его прав. Соседние сценарии, которых правка могла коснуться: запись вошедшей клиенткой, оформление из панели, видимость визита гостя в панели, недоступность чужой записи гостю, регистрация по тому же телефону с сохранением истории визитов. Путь пройден целиком и в браузере.

## 30. Разбор находок прогона (добавлено 4 сентября 2026)

Прогон чек-листа дал три находки. Каждая исправлена отдельной правкой и отдельным коммитом, после каждой пройдены соседние сценарии.

| Что было сломано | Причина | Как исправили |
|---|---|---|
| Гость не мог записаться: доходил до подтверждения и упирался в кнопку «Войти», прямой запрос отвечал 401 | `POST /api/appointments` начинался с `requireRole(req, 'client')` — ветку для гостя не собрали, когда делали клиентскую часть | Вторая ветка адреса: без пропуска сервер ждёт имя, телефон и согласие, находит человека по телефону или заводит с пустым паролем. Сеанс не выдаётся, роль всегда клиентская. Подробно — раздел 29 |
| На экране «Записались» под ценой и под адресом печаталось слово «null» | `replaceChildren` не пропускает `null` мимо, как это делает наш `el`, а превращает его в текстовый узел. На четвёртом шаге завели `fill` и обошли четыре места, `booking-done.js` в тот обход не попал | `replaceChildren` заменён на `fill`. Заодно обошли все вызовы в `web/js` разбором скобок, а не поиском по строкам: больше `null` на верхний уровень нигде не приходит |
| `/account`, `/profile`, `/notifications` приходили гостю с кодом 200, на вход уводил уже скрипт страницы | Серверная проверка стояла только на `/admin`; клиентские страницы полагались на скрипт, который выполняется после отдачи страницы | Тот же список закрывает и их: семь адресов кабинета отвечают гостю 302 на `/login` с адресом возврата. Страницы записи в список не попали намеренно — по ним гость проходит до конца |

**Группа «портят данные или открывают чужое» оказалась пуста**, и это проверено отдельно, а не принято на веру: чужая запись по номеру в адресе отвечает 403, административные адреса клиентке недоступны, конфликт двух одновременных записей разрешает база, роль себе не назначить ни регистрацией, ни внешним входом, пропуск в браузере не хранится.

**Косметических находок в остатке нет.** Все три исправлены: две из них были косметическими, и обе оказались правками в одну-две строки — откладывать было дороже, чем сделать.

**Регрессия после всех правок:** проверки третьего степа 21 из 21, четвёртого 36 из 36, пятого 26 из 26, проверки гостя, клиента и безопасности — без новых сбоев. Полный цикл «удалить базу, накатить миграции, загрузить сиды, поднять сервер, пройти запись» отработан с нуля.

**Что важно сказать о самом прогоне.** Его выполнял ассистент — запросами к серверу, чтением базы и работой в браузере, в том числе на ширине 375 пикселей. Это не заменяет человеческий взгляд: понятность формулировок, удобство и вкус к вёрстке так не проверяются, и часть сценариев стоит пройти глазами.

Отдельно стоит отметить, что четыре «сбоя» первого прогона оказались ошибками самих проверок, а не сервиса: неверные имена полей в запросах (`desired_date` вместо `date_from`, `token` вместо `hold_token`, `repeats` вместо `occurrences`) и ожидание занятых окон от адреса, который по замыслу отдаёт только свободные. Каждый разобран до причины прежде, чем что-то править в коде, — иначе тремя правками сломали бы работающее.

## 31. Кнопка, обещавшая невозможное (добавлено 4 сентября 2026)

Находка Н-4. Всплыла не в прогоне чек-листа, а при ручном проходе сценария Б-9 «два клиента на одно окно» — в ожидаемом результате этого пункта не было, потому и не искали.

**Что было сломано.** Второй клиент доходит до подтверждения, видит «Так нельзя. Это время уже занято или удерживается» и список из пяти свободных окон — а кнопка «Подтвердить запись» под этим сообщением остаётся активной. Нажатие даёт второй отказ, теперь «Это время только что заняли. Выберите другое окно». Записи не создаётся, но человек жмёт кнопку и получает отказ дважды.

**Причина.** Отказ приходит в двух местах, и оба оставляли кнопку живой. При открытии экрана окно удерживается запросом `POST /api/holds`; если время уже занято, `holdSlot` ловил отказ и звал `onConflict` — а кнопку никто не трогал, она была активна с самого начала. Второе место — обработчик самой кнопки: он начинался со строки `submit.disabled = false` в блоке `catch`, то есть возвращал кнопку в строй при любом отказе, включая 409.

**Как исправили.** `onConflict` теперь гасит кнопку сам — одно место на оба случая. Обработчик кнопки перестал возвращать её при 409 и возвращает только при поправимых отказах: пропала связь, комментарий длиннее тысячи знаков. Кнопка оживает не от отказа, а от выбора другого окна: нажатие на предложенное время перезагружает страницу, и всё начинается заново с живым удержанием.

**Как проверили.** Повторили сценарий целиком: Марина заняла 5 сентября 11:00, Оксана дошла до подтверждения того же окна — сообщение то же, кнопка заблокирована. Затем проверили соседнее, что правка могла сломать: выбор предложенного окна 12:30 вернул кнопку, удержание пошло, запись оформилась — «Записались, №1048». То есть отказ больше не тупик, а развилка.

## 33. Домен и режим работы с данными (добавлено 8 сентября 2026)

Сервис до сих пор жил только на рабочей машине: показать его можно было, лишь открыв ноутбук. Первый шаг к публикации — адрес, по которому его найдут люди и внешние сервисы.

**Домен: `nogotochki.example`.** Регистратор — Beget (`BEGET-RU` в реестре). Зарегистрирован 8 сентября 2026, **регистрация оплачена до 8 сентября 2027**; после этой даты домен ещё месяц держится за нами и освобождается 9 октября 2027. Дата вынесена сюда именно затем, чтобы через год не искать её в почте: напоминание регистратора придёт на почту кабинета и потеряется среди рассылок, а в журнал проекта мы заглянем.

Имя домена — условное `nogotochki.example`: настоящий адрес подставляется при публикации.

**Делегирование оставлено регистратору.** В реестре стоят шесть NS-серверов Beget (`ns1/ns2` в зонах `.com`, `.pro`, `.ru`). Ничего не переносим: DNS-панель уже в том же кабинете, где домен, и менять там нечего.

**Идентификация администратора через Госуслуги — не завершена.** В реестре домен помечен `REGISTERED, DELEGATED, UNVERIFIED`: последнее слово означает, что личность администратора ещё не подтверждена. С 1 сентября 2026 для доменов `.RU` это обязательная процедура. Ни один шаг публикации она не блокирует — упрётся только продление через год.

**A-записи уже стоят, но не на наш сервер.** Первые минуты после регистрации авторитетные серверы Beget отвечали «имени не существует» — зона ещё не наполнилась. Через несколько минут появились обе записи, `@` и `www`, обе на `45.130.41.195`. Это **парковка регистратора**: обратная запись адреса — `ssl.mayday.beget.com`, по HTTP оттуда приходит страница «Новый сайт успешно создан и готов к работе».

Отсюда практическое следствие для шага с сервером: когда появится IP, эти две записи надо **исправить, а не добавить рядом третью**. Две A-записи на одно имя — это не «старая и новая», а две равноправные: DNS начнёт отдавать то один адрес, то другой, и сервис будет открываться через раз. Ошибка тем неприятнее, что выглядит как «иногда тормозит», а не как поломка.

HTTPS по адресу пока не отвечает — сертификата нет и взяться ему неоткуда: его выпускают на домен, который уже указывает на наш сервер.

**Решение по персональным данным: учебный режим.** Сервис работает на вымышленных и собственных данных владельца. Чужие настоящие данные в него не заводим — даже с устного разрешения знакомого. Реальных клиентов записываться не приглашаем. Промежуточного состояния «пока немного настоящих» не существует: как только в базе окажется хоть один реальный человек, возникают обязанности оператора персональных данных целиком — с политикой обработки, согласием на форме и уведомлением в Роскомнадзор.

Отсюда два следствия для кода: на главной нужна заметная плашка «Учебный проект. Не вводите настоящие данные» — чтобы случайный посетитель понял, куда попал, **до** того как введёт телефон; а после защиты стенд гасится, база скачивается себе.

## 34. Подготовка к запуску в контейнере (добавлено 8 сентября 2026)

На сервере нет ничего из того, благодаря чему проект работал на рабочей машине: ни установленной версии Node, ни созданного когда-то файла базы, ни `.env`. Есть голая система и код из репозитория. Поэтому условия запуска перечислены явно, в файле рядом с кодом.

**Что оказалось уже готово.** Порт берётся из `PORT`, путь к базе — из `SQLITE_PATH`, каталог под базу создаётся сам (`mkdirSync` с `recursive` в `env-check.js`), а `resolve` принимает и абсолютный путь. Эти три пункта переезда не потребовали ни строчки.

**Главная правка — миграции.** Раньше их применяла отдельная команда, которую набирал человек. На сервере набирать некому: обновление идёт само после коммита. Прогон вынесен в `server/src/migrations.js`, и зовут его теперь двое — команда `npm run migrate` и сам сервер при запуске, до первого запроса. Повторное применение исключено: применённое записано в `schema_migrations`. Отказ миграции валит запуск намеренно — сервер с недокаченной схемой отвечал бы ошибками на половину адресов, и разбираться пришлось бы по ним, а не по одному понятному сообщению.

**База переезжает в `/app/storage`.** Путь задан в `Dockerfile` через `SQLITE_PATH`, папка отдельная и пустая — в ней не должно быть ничего, кроме базы, потому что снаружи подключается папка целиком. **Этот путь нужен дословно при настройке тома.**

**Образ.** `node:24-slim` — та же версия, что и на машине разработки: база работает через встроенный `node:sqlite`, а он перестал требовать флаг только в 22.13, на `node:20` сервис не поднимется. Ни `npm ci`, ни `npm install` в сборке нет — зависимостей у проекта ноль, и ставить нечего. Процесс запускается от пользователя `node`, не от root. Есть проверка живости по `/api/health`.

**Что не копируется в образ** — перечислено в `.dockerignore`. Первой строкой локальная база вместе с папкой: уехав в образ, она легла бы поверх настоящей и затёрла записи клиентов, причём при обычном обновлении и без единой ошибки в логе.

### Проверено сборкой, а не чтением

Docker поставлен локально, образ собран и запущен. Три случая:

| Что делали | Что вышло |
|---|---|
| контейнер + том, база пустая | миграции применились сами, все 10, здоровье 200 |
| снесли контейнер, подняли новый на **том же томе** | данные на месте: 7 пользователей, 10 услуг, 5 записей; миграции повторно не применялись |
| тот же образ **без тома** | 0 / 0 / 0 |

Третья строка — то, ради чего всё это. В логе при этом **ни одной ошибки**, `/api/health` отвечает 200, контейнер помечен `healthy`. Сервис работает и пуст. Отсюда правило приёмки: деплой считается успешным не когда сервис открылся, а когда он пережил второй деплой.

Проверено и содержимое образа: внутри только `server`, `web` и пустая `storage`. Базы, `.env`, `.git` и прототипа там нет.

### Настройки, которые задаются на сервере

Задать обязательно:

| Переменная | Значение | Почему |
|---|---|---|
| `SQLITE_PATH` | `/app/storage/nogotochki.db` | уже в `Dockerfile`; **папку `/app/storage` подключить томом**, иначе база исчезнет на втором деплое |
| `TRUST_PROXY` | адрес прокси, завершающего TLS | без неё в рабочем режиме сервер **отказывается стартовать** — проверено, выход с кодом 1 |
| `SEED_PASSWORD` | свой пароль | если загружать демо-данные на боевом |

Есть разумное значение по умолчанию: `PORT` (3000), `NODE_ENV` (`production`), `TZ` (`Europe/Moscow`) — все три заданы в `Dockerfile`; `TLS_KEY_FILE` и `TLS_CERT_FILE` не нужны, TLS завершает прокси; `ALLOW_INSECURE_HTTP` не включать — это обход защиты, а не настройка.

**`YANDEX_LOGIN_STUB` на боевом оставить пустым.** Заглушка входа пускает без пароля — на опубликованном сервисе это открытая дверь.

## 35. Доверие к прокси по подсети (добавлено 8 сентября 2026)

Всплыло при проверке контейнера, до всякого сервера. `TRUST_PROXY` сравнивал адреса строка в строку: список точных IP, без подсетей. Для машины с nginx это нормально, для контейнера — нет. Обратный прокси приходит с адреса docker-сети, и адрес этот меняется при её пересоздании.

**Чем это опасно.** Конфигурация прожила бы до первой пересборки сети, а дальше одно из двух. Либо сервер вовсе перестал бы стартовать — в рабочем режиме он отказывается подниматься без TLS и без доверенного прокси. Либо, что хуже, поднялся бы и молча перестал узнавать прокси: кука сеанса теряет `Secure`, а `X-Forwarded-For` игнорируется, и ограничение частоты начинает считать всех клиентов одним адресом. Второе не видно ни в логе, ни на экране.

**Что сделано.** Запись в `TRUST_PROXY` теперь бывает двух видов: точный адрес и подсеть IPv4 (`172.16.0.0/12`). Разбор простой — адрес переводится в число, сравнение идёт по маске. Для IPv6 остаётся сравнение целиком: docker-сети адресуются IPv4, а наполовину поддержанная запись хуже честно неподдержанной, поэтому непонятная запись не отбрасывается молча, а печатает предупреждение при запуске.

**Заодно закрылась дыра того же рода.** Проверка «в рабочем режиме TLS кто-то завершает» считала записи как есть, не разбирая их. То есть один опечатанный `TRUST_PROXY` проходил эту проверку, не давая доверия ни одному адресу: сервер поднимался в рабочем режиме, считая, что за ним прокси, — а куки уходили без `Secure`. Теперь считаются только разобранные записи, и `TRUST_PROXY` из одной битой строки честно валит запуск.

**Как проверено.** 18 проверок разбора: границы подсети сверху и снизу, `/32`, `/0`, форма `::ffff:`, список из подсети и точного адреса, битая маска, мусор, IPv6 целиком. Затем в контейнере, с настоящим адресом docker-сети: с покрывающей подсетью вход ставит куку с `Secure`; без заголовка `X-Forwarded-Proto` — не ставит; с **чужой** подсетью заголовку не верят и `Secure` не появляется; `TRUST_PROXY` из одной битой записи даёт отказ запуска с кодом 1.

## 36. Сервер и панель развёртывания (добавлено 8 сентября 2026)

**Сервер.** VPS у Beget, арендован 8 сентября 2026, тарификация посуточная. Конфигурация: 2 ядра, 4 ГБ памяти, 40 ГБ диска, Ubuntu 24.04.4 LTS, готовый образ с предустановленным Docker (29.8.0). **IP-адрес: `159.194.255.54`.**

Взята не минимальная конфигурация, а следующая: минимум для панели — 2 ядра и 2 ГБ, но самая прожорливая фаза здесь сборка образа, а нехватка памяти проявляется не сообщением об ошибке, а обрывом на середине. Разница в шесть рублей в сутки покупает отсутствие этой поломки.

**Файл подкачки — 4 ГБ**, размером с память сервера, до установки панели, а не после первого падения. Запись в `/etc/fstab` проверена не перезагрузкой, а `swapoff` с последующим `swapon -a`: это ровно то, что система делает при старте.

**Coolify 4.3.18** установлен, шесть контейнеров подняты и здоровы: сама панель, база, redis, realtime, sentinel и `coolify-proxy` — обратный прокси, через который пойдёт сервис.

### Две вещи, которые едва не стоили дорого

**Первая — где выполнять команды.** Первая попытка установки ушла в локальный WSL, вторая — на виртуальный хостинг Beget вместо VPS. Второе распознаётся по трём признакам сразу: имя машины `mayday` (та же, что отдаёт парковку домена), 754 ГБ памяти в `free -h` — общая машина на много клиентов, и отсутствие `sudo` и `swapon` — прав администратора там не выдают. Признак, по которому надо сверяться каждый раз, один: строка приглашения. `root@` и короткое имя машины — сервер, всё остальное — не сервер.

**Вторая — диапазон адресов Docker.** Готовились задать `TRUST_PROXY=172.16.0.0/12`, стандартный диапазон docker-сетей. Установщик Coolify на шаге 4/9 прописал в `/etc/docker/daemon.json` свой пул: `{"base":"10.0.0.0/8","size":24}`. Значит прокси придёт к приложению с адреса `10.x.x.x`, и **верное значение — `TRUST_PROXY=10.0.0.0/8`**. Со старым сервис в рабочем режиме не поднялся бы вовсе, а в лучшем случае поднялся бы и молча потерял `Secure` на куке сеанса. Диапазон надо смотреть в `daemon.json`, а не брать из общего знания.

**Зеркало реестра образов** прописано после установщика, а не до: он перезаписывает `daemon.json` целиком, и зеркало, прописанное заранее, из него молча исчезает. Добавлены `https://dockerhub1.beget.com` (внутри сети провайдера, потому первым) и `https://mirror.gcr.io` запасным, поверх существующих настроек, а не вместо них. Конфиг проверен `dockerd --validate` до перезапуска демона — работающий Docker всё это время жил на старом файле. Применено через `systemctl reload`, а не `restart`: перезапуск погасил бы все контейнеры Coolify вместе с портами 80 и 443. Результат сверен по `docker info`, а не по содержимому файла: файл с ошибкой Docker просто игнорирует.

Отдельно стоит сказать, что Docker Hub с адреса этого сервера пока отдаёт образы и напрямую — проверено `docker pull` до настройки зеркала. Зеркало всё равно поставлено: доступность может пропасть в любой момент, а обнаруживается это посреди сборки.

## 37. Первое развёртывание из репозитория (добавлено 8 сентября 2026)

Сервис работает в интернете: `http://159.194.255.54:3000`. Разворачивается панелью Coolify по `docker-compose.yaml` из репозитория, ветка `main`.

**Настройки живут в проекте, а не в панели.** В панели заполнены ровно два поля: репозиторий и две переменные с учётной записью администратора. Всё остальное — путь к базе, порт, рабочий режим, доверие к прокси, выключенная заглушка входа — лежит в файле описания и едет вместе с кодом. Настройка, заданная мышью и нигде не записанная, теряется при первом переезде на другой сервер.

**Данные — в томе `nogotochki-data`, подключённом как `/app/storage`.** Контейнер при каждом развёртывании уничтожается и создаётся заново; база без тома уезжала бы вместе со старым.

**Секретной строки для подписи пропусков в проекте нет и не заводится.** Пропуск — это 32 случайных байта, которые сервер выдаёт при входе и хранит у себя хешем; при каждом запросе он сверяет пришедшее с сохранённым. Секрет нужен там, где сервер сеансы у себя не держит и вынужден доверять тому, что принёс браузер. Не наш случай, поэтому генерировать нечего: секрета нет ни в репозитории, ни в панели, ни в заметках.

**Учётная запись администратора заводится из окружения.** `ADMIN_EMAIL` и `ADMIN_PASSWORD` задаются в панели, в репозиторий не попадают, пароль нигде не печатается. Существующего пользователя переменные не трогают.

### Проверка, ради которой всё это

Деплой считается успешным не когда сервис открылся, а когда он пережил второй деплой. При первом запуске приложение не находит базы и создаёт её заново — и выглядит это одинаково независимо от того, лежит база в томе или внутри контейнера. Отличить нельзя в принципе.

Проверено дважды: сначала локально полным циклом, потом на боевом. На сервере: зарегистрировали клиентку, завели услугу, нажали Redeploy без единой правки — после пересборки клиентка вошла своим паролем, услуга на месте, строки «Миграции применены при запуске» в логе нет.

### Что мешало по дороге

**Адрес возврата GitHub.** Первое приложение GitHub создалось, но панель не получила его закрытый ключ: адресом возврата был домен `nogotochki.example`, где панели нет. Проявилось это не сразу, а позже — ошибкой `DECODER routines::unsupported` при попытке прочитать несуществующий ключ. Урок общий: если сервис указывает наружу свой адрес, он должен быть тем, по которому этот сервис действительно открывается.

**Ключ, приклеенный к чужой строке.** Файл `/root/.ssh/authorized_keys` от хостера заканчивался без перевода строки, и ключ, дописанный установщиком, слился с предыдущей строкой — превратившись в часть её комментария. Панель из-за этого не могла подключиться к собственному серверу. Поиск по тексту ключа находил два совпадения, хотя рабочая запись была одна.

**Расширение файла описания.** Панель по умолчанию ищет `docker-compose.yaml`, у нас был `.yml`. Переименовали файл, а не завели настройку в панели: имя ни на что не влияет, а лишнее поле в панели — это ещё одна настройка, живущая только в ней.

## 38. Домен и защищённое соединение (добавлено 9 сентября 2026)

Сервис открывается по собственному адресу: **https://nogotochki.example**, вместе с `www`. Обычный HTTP отвечает перенаправлением на HTTPS.

**Сертификаты Let's Encrypt** выпущены на оба имени, выданы 9 сентября 2026, действуют до 8 декабря. Продлевает их панель сама, руками ничего делать не нужно.

Выпуск произошёл без нашего участия: как только маршрут заработал по открытому HTTP, Let's Encrypt пришёл на 80-й порт за подтверждением и выдал сертификат. Отсюда два следствия, которые стоит помнить: порт 80 закрывать нельзя, даже когда всё работает по HTTPS, — и проверка домена в панели была не так важна, как казалось, потому что решающее слово всё равно за Let's Encrypt.

**Что мешало.** Домен сначала оказался вписан в поле URL самой панели, а не в поле Domains приложения. Разница не косметическая: панель и сервис — разные адресаты, и домен должен принадлежать сервису. Понять это удалось не по интерфейсу, а по меткам контейнера: `docker inspect` не показал ни одной метки Traefik, то есть маршрута к приложению не существовало вовсе. Второе: сохранить домен мало, нужно развернуть заново — правила для Traefik создаются вместе с контейнером, а не при нажатии Save.

Ещё раньше проверка DNS в панели отказывалась принимать домен, хотя записи были правильные. Проверено тремя независимыми путями — Cloudflare со стороны, авторитетные серверы Beget, сам сервер запросом к 1.1.1.1, — все три дали верный адрес. Помог перезапуск контейнера панели: у неё был закеширован прежний ответ, полученный до смены A-записи.

### HSTS терялся на страницах

Нашлось при проверке боевого адреса: заголовок `Strict-Transport-Security` приходил на ответах API и отсутствовал на страницах.

**Причина.** Заголовок подмешивался в набор, который добавляет отправщик ответов API. Страницы отдаёт `static.js` — он пишет заголовки сам и этого набора не видит.

**Что сделано.** Заголовок ставится через `res.setHeader`: такие заголовки Node сливает с любым `writeHead`, поэтому он появляется на обоих путях. Страницы здесь важнее API: HSTS запрещает браузеру ходить на этот адрес по открытому HTTP, а браузер ходит именно на страницы.

**Проверено** на собранном образе с подходящей подсетью в `TRUST_PROXY`: заголовок появился на `/`, `/login`, `/404` и `/api/health`. Без `X-Forwarded-Proto` от доверенного прокси его по-прежнему нет — на открытом соединении он бессмыслен, а браузер запомнил бы правило и для локального стенда.

Попутно подтвердилось, что доверие к прокси по подсети работает как задумано: при локальной проверке с сетью `172.x` и настройкой `10.0.0.0/8` сервер отправителя не признал и заголовок проигнорировал.

### Проверка боевого адреса

19 проверок, сбоев нет. Страницы `/`, `/login`, `/register`, `/booking`, `/password-reset`, `/404`; закрытые `/account`, `/profile`, `/admin/` уводят на вход; адреса API отвечают. Вход клиентки проходит, чужой пароль отклоняется с 401 «Неверный логин или пароль». **Кука сеанса приходит с `Secure`, `HttpOnly` и `SameSite=Lax`** — то есть `TRUST_PROXY=10.0.0.0/8` подобран верно, сервер узнаёт прокси Coolify. Заглушка входа через Яндекс выключена: список способов входа пуст, прямой запрос отвечает 409.

## 39. Плашка учебного проекта (добавлено 9 сентября 2026)

Решение об учебном режиме записано ещё в разделе 33, а следствие для кода до сих пор оставалось невыполненным: сервис уже открыт в интернете, но нигде не сказано, что он тренировочный.

**Где стоит.** На главной — выше первого экрана, до всякого текста о студии. На регистрации — над формой, а не под ней. На шаге подтверждения записи — там гостья оставляет имя и телефон, не заводя кабинета.

**Почему именно так.** Смысл предупреждения в том, чтобы человек понял, куда попал, **до** того как введёт телефон. Плашка внизу страницы или после отправки формы этой задачи не решает: данные к тому моменту уже введены.

Текст разный на трёх экранах намеренно. На главной — общий смысл, на регистрации — что придумать вымышленные данные, на подтверждении — что студия по этой записи никого не ждёт. Одинаковый текст в трёх местах читается как шаблон, и его перестают замечать после первого раза.

Стиль — из токенов, отдельным классом в `base.css`; инлайновых стилей нет, их запрещает политика безопасности страниц. Проверено на собранном образе и в браузере, в том числе на ширине 375 пикселей.

## 40. Настоящий Яндекс вместо заглушки (добавлено 9 сентября 2026)

Заглушка входа удалена из проекта вместе с переменной `YANDEX_LOGIN_STUB`. На её месте — обращение к Яндексу. Предупреждение из раздела 27 закрыто.

**Половина работы отработала так, как задумывалась.** Когда вход собирали, поход наружу вынесли в отдельную функцию `yandexProfile` и написали над ней, что именно тут появится настоящий Яндекс. Сейчас пришлось переписать её внутренности и добавить два адреса — всё остальное осталось нетронутым: поиск человека по паре «сервис + идентификатор», привязка к учётной записи с паролем, роль `client` буквальным значением, выдача собственного пропуска. Ни строчки.

**Два адреса вместо одного.** Раньше заглушке хватало одного `POST /api/auth/yandex`: она возвращала почту сразу. Живому входу нужен переход — `GET /api/auth/yandex/start` уводит человека на страницу согласия, `GET /api/auth/yandex/callback` принимает его обратно с одноразовым кодом. Отвечают они перенаправлениями, а не JSON: сюда приходит браузер человека, а не скрипт страницы.

**Кнопка стала ссылкой, и это не косметика.** Вход через Яндекс — уход на чужой сайт, а не запрос из скрипта: браузер должен выполнить обычный переход, чтобы потом вернуться с кодом.

**Защита от подложенной ссылки.** При уходе сервер кладёт случайную строку в куку и её же отправляет Яндексу; при возврате сверяет. Не совпало — вход не состоится. Иначе человеку можно было бы подсунуть ссылку с чужим кодом и войти в его браузере под собой.

**Токен Яндекса не сохраняется нигде.** Он нужен на один запрос за почтой и именем. Хранить его — значит отвечать за доступ к чужому аккаунту: утечка нашей базы стала бы утечкой доступа к его Яндексу.

**Идентификатор и секрет — только из окружения, без значений по умолчанию.** Запасное значение у секрета означало бы секрет, лежащий в репозитории. В `.env.example` обе строки пустые, с объяснением, что секрет задаётся в панели развёртывания.

### Отказ больше не белый экран

Раньше отказ на экране согласия не был предусмотрен вовсе. Теперь любая ветка возврата заканчивается экраном входа с объяснением: человек нажал «Отмена», Яндекс вернул ошибку, не совпала строка состояния, обмен кода не удался, вход не настроен. Сервер называет только повод, формулировку показывает страница — тексты живут в одном месте.

**Проверено на собранном образе, все пять веток:**

| Что делали | Куда вернуло |
|---|---|
| нажали «Отмена» | `/login?yandex=denied` |
| Яндекс вернул другую ошибку | `/login?yandex=error` |
| подложили чужую строку состояния | `/login?yandex=state` |
| строка состояния есть, кода нет | `/login?yandex=error` |
| настоящий поход в Яндекс с выдуманным кодом | `/login?yandex=error` |

Последняя строка — не имитация: запрос действительно ушёл на `oauth.yandex.ru`, тот ответил `invalid_client`, причина легла в лог сервера, а человек получил чистый возврат. То есть проверена и та ветка, которая ходит наружу.

Без настроек кнопка не рисуется вовсе, а прямой заход на адрес ухода отвечает перенаправлением на `/login?yandex=off`.

### Кнопка по требованиям Яндекса

Кнопка была сделана в стиле проекта — это было верно, пока входа не существовало. Теперь она приведена к требованиям: чёрный фон, белый текст «Войти с Яндекс ID», знак слева.

**Здесь единственное место в проекте, где цвета написаны цифрами, а не взяты из `tokens.css`.** Конфликт правил разрешён в пользу Яндекса сознательно: элемент чужого бренда встраивают, а не перерисовывают. Человек не читает надпись — он узнаёт форму и цвет за долю секунды. Кнопка в наших фирменных цветах заставила бы его читать и на секунду усомниться, куда его уводят.

Что осталось нашим: скругление (его менять разрешено, и кнопка садится в композицию соседних экранов), обводка фокуса — она про доступность нашего интерфейса, а не про чужой бренд, — и всё, что вокруг: ширина колонки, отступы до соседей. Новых значений в `tokens.css` не добавлено, файл не тронут.

**Оговорка про знак.** Красный кружок с буквой «Я» — приближение, а не официальный файл. Настоящий знак Яндекс ID нужно взять из их руководства по фирменному стилю и подставить вместо этого элемента. Рисовать чужой логотип по памяти хуже, чем сказать об этом вслух.

### Ключи доехали не с первого раза, и причина была в нашем файле

После первого развёртывания с заданными в панели переменными вход остался выключенным. Внутри контейнера картина оказалась говорящей: имена переменных есть, значения пустые.

**Причина — строка вида `- YANDEX_CLIENT_ID=${YANDEX_CLIENT_ID:-}` в файле описания.** Если панель не подставила значение при разборе файла, такая запись задаёт контейнеру **явную пустую строку**, и она перебивает то, что панель добавляет от себя. Переменная, которую снаружи задали правильно, внутрь приходила пустой.

**Правило, которое из этого следует: пустое объявление сильнее отсутствующего.** Переменную, приходящую снаружи, в файле описания лучше не упоминать вовсе. Три строки убраны, на их месте объяснение. `ADMIN_EMAIL` и `ADMIN_PASSWORD` оставлены как были — они доезжают правильно, и трогать работающее ради единообразия незачем.

Отдельно стоит назвать, что помогло найти причину: не догадки, а `docker exec … env | grep -i yandex`. Снаружи это выглядело как «ключи не заданы», и разбирать можно было бы панель, кабинет Яндекса и код. Один взгляд внутрь контейнера показал точную картину — имена без значений, — и она сразу указала на подстановку.

### Проверка на боевом

Со стороны, без доступа к аккаунту: кнопка появилась, адрес ухода ведёт на `oauth.yandex.ru/authorize` с верными `client_id` и `redirect_uri`, строка состояния выдаётся. Запрос по этому же адресу Яндекс принял — перенаправил на свою страницу входа, сохранив наш запрос в `retpath`. Выдуманные ключи он до этого отвергал сразу.

Живой вход выполнен владельцем сервиса под настоящим Яндекс ID. **Главная проверка шага пройдена:** почта совпала с почтой заведённого администратора, и вход привязался к существующей учётной записи, а не завёл вторую.

| Что проверяли | Что в базе |
|---|---|
| второго кабинета на ту же почту нет | всего пользователей 1 |
| роль не изменилась | `admin` — Яндекс её не выдавал и не мог |
| внешний вход записан | `provider: yandex`, `provider_id: 9012117` |
| пароль на месте | `password_hash` не пуст, вход по паролю работает наравне |

Два способа входа ведут к одному человеку, а не к двум разным.

## 41. Логотип студии на сайте (добавлено 9 сентября 2026)

До сих пор в проекте не было ни одного изображения: на лендинге вместо фотографий стоят рамки-заглушки, а логотип в шапке был набран шрифтом. Владелец прислал файл логотипа — JPEG, 703×383, знак и надпись на бежевой подложке с тенью листвы.

**Взят только знак, надпись оставлена типографикой.** Причина не в лени: набранная шрифтом дизайн-системы, она остаётся резкой на любом экране и в любом размере, а растровая замылилась бы. Плюс тень листвы на исходнике перекрывается с надписью — вырезать её без потерь нельзя, а знак лежит в чистой части картинки.

**Как вырезан фон.** Заливка от краёв: пиксель считается фоном, если он светлый и малонасыщенный, и до него можно дойти от края, не пересекая рисунок. Глобальное правило «убрать всё бежевое» не годилось — розовая «В» тоже малонасыщенная и исчезла бы вместе с фоном.

Несколько подходов ушло на то, чтобы понять границы: сначала фон убрался вместе с сохранившейся тенью, потом обрезка по надписи подрезала саму надпись. Помогло измерение — вычислили рамку тёмных пикселей (надпись занимает x 200–506, y 285–355) и по ней поняли, где заканчивается знак и начинается текст.

**Что осталось.** По краям знака остались редкие светло-бежевые точки — следы тени, которые заливка не достала. При высоте 40 пикселей в шапке их не видно, проверено в браузере на десктопе и на 375 пикселях. Если понадобится безупречный вырез — нужен исходник с прозрачностью от дизайнера, из JPEG с текстурой чище не выйдет.

**Значок вкладки.** Его у сайта не было вовсе — браузер показывал пустой лист. Сделан из того же знака, 64×64, и подключён на всех 19 страницах.

**Мелочь по дороге.** В списке типов файлов сервера был `.jpg`, но не `.jpeg` — исходник логотипа с таким расширением не отдался бы вовсе. Добавлено.

**Раскладка со знаком ограничена шапкой.** Тот же класс `.logo` стоит в подвале лендинга, но там внутри просто текст: общий `flex` развернул бы название и подпись в строку вместо двух этажей.

## 42. У каждой демо-записи свой пароль (добавлено 9 сентября 2026)

Демонстрационные данные не уезжали на боевой сервер именно из-за паролей: сиды ставили один и тот же пароль всем учётным записям, а значение по умолчанию — `nogotochki-demo` — было напечатано в README. На опубликованном сервисе это означало бы, что войти администратором сможет любой, кто прочитал документацию.

**Что сделано.** Общего пароля больше нет. Каждой учётной записи выдаётся свой случайный, он нигде не сохраняется и по умолчанию не печатается. Переменная `SEED_PASSWORD` убрана.

Для локального стенда список показывается по требованию: `SEED_SHOW_PASSWORDS=true npm run seed` печатает таблицу «логин — пароль» один раз, при загрузке. Без флага сиды сообщают, что пароли случайны и войти под демо-записями нельзя, — на сервере это и нужно: мастера там нужны как данные, к которым записываются, а не как способ войти.

**Баг, найденный по дороге, — и он был ровно того рода, который сам себя не покажет.** Первая версия правки печатала пароли, которых не записывала: `ON CONFLICT` обновлял только имя, и при повторном запуске новый пароль генерировался, показывался и терялся. Проверка вскрылась не чтением кода, а попыткой войти по напечатанному паролю — сервер ответил 401. Теперь `ON CONFLICT` обновляет и хеш пароля: напечатанное обязано работать.

Заодно это делает поведение честным и предсказуемым: сиды всегда переустанавливают пароли демо-записей. Учётные записи, заведённые людьми, — как администратор студии — сиды по-прежнему не трогают.

**Проверено на собранном образе:** пять записей с паролем, пять разных хешей, совпадений ноль; вход по напечатанному паролю проходит; старый `nogotochki-demo` не подходит; каталог наполняется — 10 услуг, 3 мастера, портфолио, график.

Обновлены README, `.env.example` и вводная часть чек-листа: везде, где обещался общий пароль.

## 43. Автодеплой и демо-данные на боевом (добавлено 9 сентября 2026)

**Демонстрационные данные загружены на сервер.** Было пусто: 4 рубрики прайса из миграции 011 и ничего больше. Стало 10 услуг, 3 мастера, график работы, портфолио и тексты первого экрана. Учётная запись владельца сервиса не пострадала — сиды её не трогают.

Загружать их раньше было нельзя именно из-за паролей, и это исправлено отдельно (раздел 42). Проверено на боевом: вход по прежнему общему паролю не проходит ни под администратором, ни под владелицей, ни под клиенткой — везде 401.

**Плашка учебного режима дополнена.** Раньше она предупреждала только про личные данные. Теперь на сайте полноценный прайс с ценами и мастерами, он выглядит настоящим, и плашка говорит прямо: услуги, цены и мастера вымышленные. Заодно у лендинга появилось краткое описание для поисковиков и мессенджеров и запрет индексации: сервис учебный, живёт до защиты, и всплывать потом годами по запросу «ногтевая студия Санкт-Петербург» ему незачем.

### Автодеплой: заработал, и разбор занял больше, чем сама починка

**Итог:** пуш в `main` доезжает до боевого адреса **за 42 секунды**, без единого нажатия в панели. Проверено по-настоящему: правка страницы, коммит, пуш — и слежение за боевым адресом до момента, когда изменение появилось само.

Дорога к этому состояла из трёх неверных версий, и каждая казалась убедительной:

**Версия первая: не хватает порта.** Панель живёт на `:8000`, а в её поле URL стоял адрес без порта. На возврате от Яндекса это ровно та ошибка, что мы уже ловили, — и версия выглядела очевидной. **Оказалась неверной:** проверка показала, что приёмник веб-хуков отвечает `200` и на 80-м порту тоже, Traefik его пропускает.

**Версия вторая: не тот путь.** Панель показала во вкладке Webhooks адрес `https://nogotochki.example/post-receive` — на домене сервиса, а не своём. Там отвечает наше приложение, и GitHub получал `404`. Настоящий приёмник нашёлся перебором: несуществующие пути панель уводит на `/login`, а `/webhooks/source/github/events` отвечает `200`. **Версия верная, но недостаточная:** доставка стала успешной, а сборка всё равно не начиналась.

**Версия третья: адрес развёртывания через API.** Следующая попытка вела на `/api/v1/deploy?uuid=…`, и GitHub получал `401`. Причина в природе адреса: он требует токен в заголовке, а веб-хук GitHub заголовков не добавляет. Такой путь не заработал бы никогда.

**Что помогло на самом деле:** убрать лишний веб-хук репозитория и оставить связь через приложение GitHub, у которого приёмник свой. После этого сборка пошла сама с первого же пуша.

**Урок, который стоит унести.** `200` в ответе на веб-хук означает «сообщение получено», а не «принято к работе». Панель может согласиться и не сделать ничего. Проверять надо не код ответа, а результат — появилась ли сборка и доехало ли изменение до адреса. Именно поэтому проверкой служила правка видимого текста, а не запись в логе.

## 44. Приёмка боевого сервиса (добавлено 9 сентября 2026)

**Сервис опубликован и принят.** Адрес — <https://nogotochki.example>, опубликован 8 сентября 2026, домен оплачен до 8 сентября 2027.

**Решение по домену: дать истечь.** Продлевать не будем — **8 сентября 2027 регистрация закончится**, и домен освободится сам, без каких-либо действий. Вернуть потом это же имя почти наверняка не выйдет: освободившиеся разбирают быстро. Решение осознанное, а не забывчивость: сервис учебный и живёт до защиты.

**Прогон пройден на боевом адресе**, результаты — в [prod-checklist.md](prod-checklist.md). Семнадцать сценариев, отобранных из 67 по одному признаку: что именно мог сломать переезд. Пятнадцать прошёл ассистент запросами к боевому сервису и работой в браузере, включая ширину 375 пикселей; живой вход через Яндекс прошёл владелец под своим аккаунтом. Сбоев нет.

**Один сценарий остался непроверенным честно, а не округлённо.** Отказ на экране согласия Яндекса: экрана нет, потому что Яндекс помнит выданное разрешение и больше не спрашивает. Серверная ветка проверена запросом, глазами — нет.

**Копия базы скачана** 9 сентября 2026, лежит вне репозитория, с датой в имени. Целостность проверена, содержимое сверено: 11 миграций, 8 пользователей, 10 услуг, 5 записей. Этим закрыт риск из паспорта продукта — «файл SQLite хранится в одном экземпляре». Снимки сервера у провайдера защищают от смерти сервера, эта копия — от собственных ошибок.

**Автодеплой проверен настоящим пушем:** правка страницы доехала до боевого адреса за 42 секунды, без нажатий в панели. Разбор занял больше самой починки и описан в разделе 43.

### Что подтвердилось на боевом и не проверялось бы локально

**Часовой пояс сошёлся трижды независимо.** Выбрали 14:00 — кабинет показывает «10 сентября в 14:00»; панель студии и кабинет дают одно значение; фильтр по завтрашнему дню не съезжает. Сервер хранит UTC и показывает московское.

**Сервер видит настоящий адрес клиента, а не адрес прокси** — записал внешний адрес в сеанс. Прямое доказательство, что `TRUST_PROXY=10.0.0.0/8` подобран верно. Без этого ограничение частоты считало бы всех посетителей одним человеком и блокировало посторонних.

**Гонка за одно окно разрешается базой, а не удачей.** Два одновременных запроса на одно время: 201 и 409 с пятью предложенными окнами. По настоящей сети промежуток между проверкой и записью шире, чем локально, — и он выдержал.

**Ограничение частоты блокирует логин, а не устройство.** Восемь попыток — отказ, девятая — запрет на 15 минут; верный пароль после этого тоже отбит, соседняя учётная запись не пострадала. Блокировка держится и для несуществующего логина — иначе по коду ответа можно было бы выяснять, какие номера зарегистрированы.

### Чего этот прогон не заменяет

С настоящего телефона по мобильному интернету никто не ходил. Ширину экрана имитировать можно, толщину пальца, скорость мобильной сети и клавиатуру, перерисовывающую страницу, — нет.

### Как погасить сервер, когда он станет не нужен

Порядок именно такой, и первый пункт не переставляется: после удаления сервера скачивать будет неоткуда.

1. **Скачать файл базы себе.** Даже если внутри вымышленные записи — это результат работы: заведённые услуги, расписание, клиентки.
2. **Удалить сервер в панели провайдера** — именно удалить, а не выключить. Тарификация посуточная, и выключенный сервер во многих тарифах продолжает списывать деньги за зарезервированные ресурсы. После удаления проверить, что его нет в списке услуг и не осталось автопродления.
3. **С доменом ничего делать не нужно** — решение уже принято и записано выше: даём истечь 8 сентября 2027. Никаких действий это не требует, но и обратной дороги не будет.
4. **Обязанностей не остаётся.** Это следствие решения из раздела 33: сервис работал в учебном режиме, на вымышленных и собственных данных. Персональных данных не было — значит нет ни оператора, ни уведомления, которое надо отзывать.

### Что осталось за рамками

Перечисление, а не план: перенос базы с SQLite на полноценную СУБД, уведомления по почте, бэкапы по расписанию в отдельное от сервера хранилище, официальный файл знака Яндекс ID на кнопке входа, поддомен для панели развёртывания.

## 32. Что дальше

Черновой фронтенд — временный: он проверяет API, а не показывает продукт. Настоящий интерфейс планируется на Vite + React с CSS дизайн-системы из `site/`.

При работе держать в голове: обращения к базе синхронные, транзакции открывать через `transaction()` из `db.js` (там `BEGIN IMMEDIATE`), время передавать строками ISO-8601 UTC, деньги — целыми копейками, а имена столбцов в `UPDATE` собирать только через `setClause()`.


