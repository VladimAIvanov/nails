# Отчёт о публикации сервиса

**Проект:** «Ноготочки», сервис онлайн-записи в бьюти-студию

> Черновик. Пункты, помеченные **[заполнить]**, требуют действий в браузере под учётными записями — их проходит владелец сервиса. Остальное проверено запросами к боевому адресу 16.09.2026.

---

## 1. Адрес и адресная строка

**Адрес сервиса:** <https://nogotochkee.ru>

**Что показывает браузер:** замок есть. Сертификат выпущен автоматически после того, как домен был указан у приложения в панели Coolify; отдельный адрес `www.nogotochkee.ru` тоже открывается.

**Если замка нет, дословный текст предупреждения:** не применимо, замок есть.

**Адрес с http:** сам переходит на https. Запрос к `http://nogotochkee.ru/api/health` отвечает `302` на `https://nogotochkee.ru/api/health`.

Дополнительно: сервис отдаёт заголовок `Strict-Transport-Security: max-age=31536000; includeSubDomains` и политику содержимого, разрешающую только собственный источник (`default-src 'self'`), включая шрифты — они лежат в `web/fonts` и не тянутся с чужих серверов.

---

## 2. Три сценария на боевом адресе

### Регистрация и вход на боевом адресе

Проверено вручную 16.09.2026: на `https://nogotochkee.ru/register` заведена клиентка **Валентина Иваненко, +7 999 666 0001** (данные вымышленные). После регистрации сразу открылся личный кабинет — отдельный вход не потребовался. Кабинет пустой: «Записей пока нет».

### Сценарий 1. Запись клиента целиком

**Имя и почта тестового клиента:** Валентина Иваненко, телефон `+7 999 666 0001` (учётная запись заведена при этой проверке, данные вымышленные, почта не указывалась — сервис её не требует).

**Что выбрал:** ламинирование бровей, мастер Марина Орлова, 18 сентября, 12:00.

**Текст экрана подтверждения, дословно:**

> Записались, №1047
>
> Визит подтверждён. Напоминание придёт заранее.
>
> Когда
> 18 сентября в 12:00
> Подтверждена
> №1047 · Ламинирование бровей
> 1 ч
> 1 800 ₽
> Мастер
> Марина Орлова
> Адрес
> Цветочная ул., 16, Третий этаж, студия 4
>
> Отменить или перенести можно в кабинете. Бесплатно — не позднее чем за 24 ч.

**Вывод:** запись создалась. Весь путь — регистрация, услуга, мастер, время, подтверждение — прошёл без заминок. В кабинете сразу появился блок «Ближайший визит»: «Ламинирование бровей, Подтверждена, 18 сентября в 12:00, Марина Орлова · 1 800 ₽ · 60 мин, Цветочная ул., 16» с кнопками «Перенести», «Отменить», «Подробнее».

Отдельно: раньше в подтверждении стоял адрес прошлой версии проекта. Перед этой проверкой адрес и телефон заменены вымышленными (миграция 016), и в тексте выше уже новые.

### Сценарий 2. Админка под обычным клиентом

**Что сделал:** оставаясь в том же окне под Валентиной Иваненко, набрал в адресной строке `https://nogotochkee.ru/admin`.

**Что появилось на экране, дословно:**

> Ошибка 403
>
> Этот раздел только для администраторов
> Вы вошли, но прав на панель студии у вашей учётной записи нет. Всё, что нужно для записи и просмотра своих визитов, — в личном кабинете.

**Откуда это:** отказ от сервера. Каркас панели не появился: сервер вернул код 403 и другую страницу, скрипты панели не отдаются. Ссылки «Админ-панель» в шапке у клиентки нет. Гостя без входа сервер уводит на страницу входа (`302` на `/login?next=%2Fadmin`).

**Вывод:** доступ закрыт на сервере, а не спрятан в интерфейсе.

### Сценарий 3. Проверка времени

**Какую запись сверял:** №1047, 18 сентября, ламинирование бровей, мастер Марина Орлова.

**Что сделал администратор:** вошёл под своей учётной записью, открыл `/admin`, поставил дату 18.09.2026, нашёл строку «12:00 · Валентина Иваненко» и перенёс визит на 14:00. Панель ответила: «Визит Валентина Иваненко перенесён на 14:00. Это та же запись с новым временем — второй в истории не появилось, клиентке ушло одно уведомление».

**Время в кабинете клиента:** «18 сентября в 14:00», запись одна — новой не появилось.

**Время в админке:** 14:00 в списке записей 18 сентября.

**Уведомление клиенту:**

> Визит перенесён
> Запись на пятница, 18 сентября, 12:00 перенесена на пятница, 18 сентября, 14:00.
> Запись № 1047

Счётчик непрочитанных изменился с 0 на 1.

**Вывод:** совпали. Время одно и то же в кабинете, в админке и в уведомлении; перенос изменил существующую запись, а не создал новую.

### Сохранность данных после повторного деплоя

**Что сделал:** нажал Redeploy в панели. Развёртывание прошло успешно, контейнер пересоздан, том с базой не пересоздавался.

**Что получилось:** запись №1047 на месте, время 14:00, услуга и мастер те же; в кабинете по-прежнему одна активная запись.

**Независимая проверка снаружи:** свободные окна Марины Орловой на 18 сентября после деплоя — 11:00, 11:30, 12:00, 12:30, 13:00, затем сразу 17:00. Окна 13:30 и 14:00 недоступны из-за записи 14:00–15:00, а 15:00–16:30 закрыты блокировкой «личное время». Освободившееся после переноса 12:00 снова доступно.

---

## 3. Вход через Яндекс

**Что показал экран согласия Яндекса:** **[заполнить]** — каким названием подписано приложение, какие права запрошены, было ли предупреждение о непроверенном приложении.

**Роль в базе:** **[заполнить]**. Ожидается `admin`: почта приложения в Coolify (`ADMIN_EMAIL`) — на Яндексе, поэтому вход привяжется к существующему администратору, а не заведёт нового клиента. Чтобы увидеть сценарий «новый пользователь стал клиентом», нужен другой Яндекс ID.

**Поле пароля:** **[заполнить]** (у администратора из `ADMIN_EMAIL` — заполнено).

**Поле provider:** **[заполнить]**, ожидается `yandex`.

**Как смотрел базу:** команда в терминале контейнера в панели Coolify:

```sh
node --input-type=module -e "const {all}=await import('./src/db.js');console.table(all(\"SELECT id, email, role, provider FROM users WHERE provider='yandex'\"))"
```

**Повторный вход:** **[заполнить]** — попал ли в тот же кабинет и не появилось ли второй строки.

Что проверено без входа: `GET /api/studio` отдаёт `"external_login":["yandex"]`; `GET /api/auth/yandex/start` отвечает `302` на `https://oauth.yandex.ru/authorize` с нашим `client_id`, адресом возврата `https://nogotochkee.ru/api/auth/yandex/callback` и случайной строкой состояния длиной 32 знака — защитой от подделки запроса.

---

## 4. Разбор одной поломки публикации

**Что видел.** После настройки автоматического развёртывания push в `main` перестал доходить до сервера. В списке доставок вебхука на GitHub — код `500`, при этом панель Coolify исправна, приложение работает, ручной Deploy разворачивает тот же коммит без единой ошибки. Первый push после создания вебхука прошёл успешно, следующие — нет.

**Чем оказалось.** В подробностях доставки GitHub написал причину дословно:

> POST https://159.194.255.54/webhooks/source/github/events/manual giving up after 1 attempt(s): tls: failed to verify certificate: x509: cannot validate certificate for 159.194.255.54 because it doesn't contain any IP SANs

То есть запрос уходил на `https://` с IP-адресом, а сертификат прокси на IP-адрес не выписан — проверить его нельзя. Сначала предполагалось, что дело в самом адресе вебхука, и он был возвращён на `http://`. Доставка всё равно падала с той же ошибкой. Проверка запросом показала настоящую причину: панель отвечает `307` и сама уводит с `http://` на `https://`, GitHub идёт по перенаправлению и упирается в тот же непроверяемый сертификат.

**Как починил.** Настоящее решение — дать панели доменное имя, на которое можно выпустить сертификат: запись `A` для `coolify.nogotochkee.ru` на сервер уже создана, остаётся указать домен в настройках экземпляра Coolify и переключить адрес вебхука на него. Пока этого нет, деплой запускается кнопкой в панели: на работу опубликованного сервиса это не влияет.

**Как убедился, что починилось:** **[заполнить после настройки домена панели]** — новый push должен дать доставку с кодом 200 и запуск развёртывания в панели.

**Чем эта поломка отличалась от тех, что случались на своём компьютере.** Она вообще не может случиться локально: на своём компьютере нет ни панели, ни вебхука, ни чужого сертификата — код просто запускается командой. Здесь же ошибка жила в стыке трёх участников: GitHub, прокси панели и сертификат. При этом ни один из них не считал себя сломанным: панель отвечала, приложение работало, логи были чистыми. Единственное место, где причина названа прямо, — журнал доставок на стороне GitHub.

Вторая поломка того же дня, для полноты: первое развёртывание падало с `fatal: Remote branch 1 not found in upstream origin`. В поле ветки в панели стояло `1` вместо `main`. Лог называл причину дословно, и правка заняла минуту.

---

## 5. Решение по персональным данным

**Режим работы сервиса:** учебный, на вымышленных и собственных данных.

**Что сделано, чтобы не собирать чужие данные:**

- **Плашка на главной**, выше первого экрана: «Учебный проект. Не вводите настоящие данные: имя, телефон и почту. Услуги, цены и мастера здесь тоже вымышленные — записаться в реальную студию через этот сервис нельзя.»
- **Отдельные предупреждения на экранах, где вводят данные:** на регистрации — «Учебный проект. Придумайте вымышленные имя, телефон и почту — настоящие вводить не нужно»; на шаге подтверждения записи — «Учебный проект. Запись тренировочная: студия по ней никого не ждёт. Оставляйте вымышленные данные».
- **Описание для поисковиков** говорит то же самое, плюс проставлен запрет индексации.
- **Кто заведён в базе.** Восемь учётных записей, живых людей среди них нет, кроме владельца сервиса. Администратор — владелец, вход по собственной почте. Демо-данные: три мастера (Анна Ковалева, Марина Орлова, Елена Смирнова), администратор `admin@nogotochkee.ru` и три клиентки (Ирина Петрова, Ольга Соколова, Дарья Волкова) — лица вымышленные, почты в домене сервиса, телефоны из ряда `+7 921 000-00-0X`. Телефон и адрес студии вымышленные: `+7 812 000-00-00`, Цветочная ул., 16.
- **Фотографии** — стоковые снимки с Unsplash под их бесплатной лицензией; авторы перечислены в `web/img/CREDITS.md`. Это не работы мастеров студии и не её интерьер, о чём сказано там же.
- **Общего пароля у демо-учёток нет:** каждой выдаётся свой случайный, он печатается один раз при загрузке данных и нигде не сохраняется.
- **Кому давал адрес:** **[заполнить]** — если ссылку никому не давали и живых клиентов не приглашали, так и написать.

**Что будет с сервером и данными после защиты:** **[заполнить]** — когда удаляется сервер (именно удаляется, а не выключается: тарификация посуточная), что делаете с файлом базы (скачать копию до удаления) и с доменом `nogotochkee.ru` (продлевать или дать освободиться). Отдельно стоит перевыпустить секрет приложения Яндекса и секрет вебхука: они назывались в переписке.

Обязанностей оператора персональных данных не возникает: сервис работал на вымышленных и собственных данных, чужих настоящих в базу не попадало.
