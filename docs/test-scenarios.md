# Сценарное тестирование сервиса «Ноготочки»

Чек-лист этапа 4 проектной работы: ключевые сценарии клиента и администратора, три контрольных сценария, ожидаемый и фактический результат. Сначала пройден весь чек-лист и записан результат, затем исправлены найденные проблемы и выполнена повторная проверка.

## Как проверялось

| Часть | Как | Где |
|---|---|---|
| Сценарии через API | скрипт `server/scripts/e2e-scenarios.mjs` поднимает отдельный сервер на временной базе, загружает демо-данные и проходит сценарии так же, как их проходят страницы | сырые результаты — `server/scripts/e2e-result.json` |
| Интерфейс | браузер, ширина 375 px (телефон): страницы, переходы, отображение | локальный сервер `http://localhost:3000` |

Запуск проверки через API из папки `server`:

```bash
node scripts/e2e-scenarios.mjs
```

### Тестовые данные

Загружаются сидами (`npm run seed`). Даты — ближайший такой день недели после загрузки.

| Клиент | Услуга | Мастер | Дата и время | Статус |
|---|---|---|---|---|
| Ирина Петрова | Маникюр с покрытием | Анна Ковалева | среда, 10:00 | подтверждена |
| Ольга Соколова | Ламинирование бровей | Марина Орлова | четверг, 12:00 | подтверждена |
| Дарья Волкова | Наращивание ногтей | Елена Смирнова | вторник, 14:00 | подтверждена |

Блокировки: Анна — четверг 13:00–14:00 (обед), Марина — пятница 15:00–17:00 (личное время), Елена — суббота весь день (выходной).

Прогон 15.09.2026. Даты в прогоне: среда 16.09, четверг 17.09, пятница 18.09, суббота 19.09, понедельник 21.09, вторник 29.09 (для новых записей — дальше суток).

## Проход 1 — до исправлений

### Контрольные сценарии

| № | Сценарий | Ожидали | Факт | Итог |
|---|---|---|---|---|
| K1 | Анна, среда, маникюр с покрытием; у Анны уже есть запись Ирины на 10:00 | не показывать время, пересекающееся с 10:00–11:30 | API: окна 11:30 … 16:30 — пересекающихся нет. **Экран выбора времени: 10:00, 10:30, 11:00 видны серыми кнопками «занято»** | API ✅ / экран ❌ (U1) |
| K2 | Марина, пятница, ламинирование бровей; блокировка 15:00–17:00 | время 15:00–17:00 недоступно | API: 11:00 … 14:00, затем 17:00 … 19:00. **Экран: 14:30–16:30 видны серыми кнопками** | API ✅ / экран ❌ (U1) |
| K3 | Два клиента одновременно занимают одно окно у Елены (вторник, 10:00) | первая запись создаётся, второй — отказ | обе видят 10:00; Ирина — 201; Ольга — 409 «Это время только что заняли. Выберите другое окно» и 5 ближайших окон. На шаге подтверждения второе удержание того же окна — 409 | ✅ |

### Обязательные сценарии

| № | Сценарий | Ожидали | Факт | Итог |
|---|---|---|---|---|
| T1 | Успешное создание записи: Мария → Анна, вторник 11:00, маникюр | запись в базе и в кабинете; занятое окно пропадает | 201; запись в кабинете Марии и в панели администратора; из окон пропали 10:00–12:00 (визит 11:00–12:30) | ✅ |
| T2 | Два клиента занимают один слот | см. K3 | см. K3 | ✅ |
| T3 | Ольга открывает, переносит и отменяет запись Ирины | отказ, запись не видна | чтение 403, перенос 403, отмена 403; в списке Ольги записи нет | ✅ |
| T4 | Клиент открывает `/admin` | содержимое панели не показывается | `/admin` и `/admin/services` — 403, страница «Раздел недоступен», скрипты панели не отдаются; API панели — 403; гость — редирект на вход; администратор — 200. Ссылка «Админ-панель» в шапке рисуется только при роли admin | ✅ |
| T5 | Отмена записи клиентом | статус «отменена», запись в истории, время свободно | 200; статус cancelled; запись в истории; 14:00 снова свободно | ✅ |
| T6 | Перенос записи клиентом с 11:00 на 14:00 | меняется время той же записи | 200; у Марии одна запись с тем же id и временем 14:00; 11:00 свободно, 14:00 занято. Перенос на 17:00 (не успевает до 18:00) — 409 | ✅ |
| T7 | Отключение услуги «Коррекция и окрашивание бровей» | не видна при новой записи, сохраняется в старой | нет в каталоге; окна и запись — отказ; в записи Ирины услуга показывается | ✅ |
| T8 | Отключение мастера Елены Смирновой | не виден при новой записи, сохраняется в старой | нет в списке; окон нет; запись — 409; в записи Ирины мастер показывается | ✅ |

### Регистрация, вход, расписание, админ-панель

| № | Сценарий | Ожидали | Факт | Итог |
|---|---|---|---|---|
| R1 | Регистрация нового клиента | аккаунт с ролью client | 201, роль client | ✅ |
| R2 | Вход клиенток и администратора | вход выполнен | все четверо вошли | ✅ |
| R3 | Личный кабинет после входа | с сессией — кабинет, без — вход | 200; без сессии — редирект на `/login?next=/account` | ✅ |
| S1 | Анна, четверг, маникюр 1,5 ч: обед 13:00–14:00, смена до 18:00 | нет окон 12:00–13:30, последнее 16:30 | 10:00 … 11:30, 14:00 … 16:30 | ✅ |
| S2 | Анна, вторник, наращивание 2,5 ч | последнее окно 15:30 | последнее 15:30 | ✅ |
| S3 | Елена, суббота с выходным | окон нет | 0 | ✅ |
| S4 | Анна, понедельник | окон нет | 0, день нерабочий | ✅ |
| S5 | Марина — наращивание | отказ | 400 «Мастер не оказывает услугу» | ✅ |
| A1 | Администратор видит записи всех клиентов | Ирина, Ольга, Дарья, Мария | все четыре | ✅ |
| A2 | Администратор переносит запись Дарьи с 14:00 на 16:00 | клиентка видит новое время | в кабинете Дарьи 16:00, пришло уведомление | ✅ |
| A3 | Администратор отменяет запись Ольги | в кабинете «отменена», окно свободно | статус cancelled; 12:00 у Марины освободилось | ✅ |
| A4 | Администратор блокирует Анне 15:00–16:00 | окна 14:00–15:30 пропадают | остались 13:30 и 16:00 | ✅ |
| A5 | Добавление услуги и правка цены | услуга в каталоге с новой ценой | 201, 200; в каталоге 550 ₽ | ✅ |
| A6 | Добавление мастера и правка имени | мастер в списке с новым именем | 201, 200; «Светлана Тестова-Иванова» | ✅ |

### Интерфейс на телефоне (375 px)

| № | Что проверяли | Ожидали | Факт | Итог |
|---|---|---|---|---|
| U0 | Открытие страниц и переходы: главная → «Записаться онлайн» → услуга → «Далее» → мастер → «Далее» → время; «Войти», «Регистрация»; `/account` и `/admin` без входа | страницы открываются, переходы работают, закрытые — через вход | все открываются; `/account` и `/admin` ведут на вход с возвратом; вёрстка шагов записи, входа и регистрации в ширину экрана | ✅ |
| U1 | Экран выбора времени | показывать только время, на которое можно записаться | сетка дня достраивается по часам студии 10:00–20:00: у Анны (смена до 18:00) видны 17:00–18:30, у Марины — время блокировки, у занятых мастеров — время чужих записей; всё это серыми кнопками с подсказкой «Это время уже занято» | ❌ |
| U2 | Две услуги, которые не делает ни один мастер (маникюр и педикюр + коррекция бровей) | понятное объяснение до перехода дальше | все мастера неактивны, но «Любой свободный» можно выбрать и нажать «Далее»; на экране времени — «Нет связи с сервером. Ни один мастер не делает выбранный набор услуг» | ❌ |
| U3 | Главная страница | без горизонтальной прокрутки | блок «Работы» шире экрана: вторая колонка карточек заканчивается на 466 px при ширине 375 px | ❌ |

Не проверялось в браузере: экраны после входа (кабинет, перенос из кабинета, админ-панель). Их поведение проверено через API (T1–T8, A1–A6); визуально их нужно пройти вручную под учётными записями из сидов.

## Найденные проблемы и исправления

### U1. На экране выбора времени видно время, на которое записаться нельзя

- **Что увидели.** Анна, среда, маникюр 1,5 ч: кроме окон 10:00–16:30 внизу серые кнопки 17:00, 17:30, 18:00, 18:30, хотя смена Анны кончается в 18:00. В день, когда у Анны запись на 10:00, серыми видны 10:00–11:00. Требование паспорта и контрольные сценарии 1–2: такое время клиенту не показывается.
- **Причина.** Сервер отдаёт только свободные окна, а экран (`web/js/booking-time.js`, функция `fullGrid`) достраивал сетку дня по часам **студии** 10:00–20:00 и все недостающие точки рисовал неактивными с подписью «Это время уже занято». Часы мастера, блокировки и чужие записи экран не различал.
- **Исправление.** Сетка больше не достраивается: экран показывает ровно те окна, что вернул сервер, разложенные по утру, дню и вечеру (`freePoints`).
- **Повторная проверка.** Анна, среда: на экране 10:00 … 16:30, после 16:30 кнопок нет — совпадает с ответом API. Анна, день с записью на 10:00: первое окно 11:30, кнопок 10:00–11:00 нет.

### U2. Набор услуг, который не делает ни один мастер, заводит в тупик

- **Что увидели.** Выбраны «Маникюр и педикюр» и «Коррекция и окрашивание бровей». Все мастера неактивны, но «Любой свободный» выбирается и «Далее» ведёт на экран времени с ошибкой «Нет связи с сервером. Ни один мастер не делает выбранный набор услуг».
- **Причина.** В `web/js/booking-master.js` карточка «Любой свободный» была активна всегда: проверка «есть ли мастер на весь набор» выполнялась только на следующем шаге, а ошибка показывалась как сетевая.
- **Исправление.** Если весь набор не делает ни один мастер, «Любой свободный» неактивен с пояснением, кнопка «Далее» выключена, подсказка предлагает вернуться и убрать одну из услуг. Сохранённый ранее выбор «любой» сбрасывается.
- **Повторная проверка.** Тот же набор: «Любой свободный» неактивен, «Далее» выключена, клик по карточке ничего не выбирает, подсказка видна. Одна услуга (маникюр): «Любой свободный» выбирается, «Далее» активна.

### U3. Главная страница шире экрана телефона

- **Что увидели.** На ширине 375 px блок «Работы» заканчивался на 466 px, страница прокручивалась вбок.
- **Причина.** В `web/css/landing.css` колонки галереи заданы как `repeat(2, 1fr)`. Нижняя граница `1fr` — ширина содержимого, а карточка с отступами 24 px и подписью «Наращивание, форма миндаль» не помещалась в половину экрана.
- **Исправление.** Колонки `minmax(0, 1fr)`, у карточки `min-width: 0`, на телефоне внутренние отступы 12 px, длинная подпись переносится.
- **Повторная проверка.** Ширина страницы 375 px, элементов за правым краем нет, колонки по 161,5 px.

## Проход 2 — после исправлений

| Что | Результат |
|---|---|
| Сценарии через API (`node scripts/e2e-scenarios.mjs`) | 26 из 26 |
| U1 — экран времени | ✅ показываются только свободные окна |
| U2 — несовместимый набор услуг | ✅ тупика нет, есть подсказка |
| U3 — главная на телефоне | ✅ без горизонтальной прокрутки |

## Что пройти вручную

Под учётными записями из сидов (`SEED_SHOW_PASSWORDS=true npm run seed`):

1. Два окна браузера (второе — в режиме инкогнито), Ирина и Ольга, одно и то же окно у Елены Смирновой → подтвердить первой, затем второй. Ожидание: у второй отказ и список ближайших окон.
2. Клиентом открыть `/admin` вручную — страница «Раздел недоступен», ссылки «Админ-панель» в шапке нет.
3. Администратором перенести и отменить запись в `/admin`, затем проверить кабинет клиентки.
4. Кабинет, перенос и отмена из кабинета, админ-панель — на ширине телефона.
